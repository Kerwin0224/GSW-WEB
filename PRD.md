# PRD: CONTEXT 语境对齐缺口修复

## Problem Statement

05-05 UIUX 主线已经落地学生、教师、管理员三角色核心页面，但仍有若干体验与 `CONTEXT.md` 的产品语境不完全一致。教师侧可见文案仍容易暴露“审计/SFT/DPO/打标”等后台治理或训练数据术语，导致教师把教学正确性核实误解成数据标注工作。管理员侧需要从“AI Native 后台”的角度补齐学校管理能力：不是复杂授权系统，而是围绕学校账号、班级关系和班级成员归属提供清晰、可操作的用户管理 MVP。

## Solution

收敛为一次最小语境对齐实现：教师侧所有可见入口、页面标题、按钮、提示和状态文案统一使用“学习记录核实”“待核实学习记录”“教学正确性核实”“本周核实覆盖”等领域语言，内部 URL、API、数据库命名可以暂时保留 audit。管理员侧新增或修正为“用户管理”入口，展示账号、角色、状态、活动和班级归属摘要，并复用 CSV 导入；班级页通过“成员分配”弹窗编辑教师和学生成员，支持教师多班、学生单班、学生自动迁班和移除最后教师的风险提示。学生单班规则已通过数据库硬约束防止复发。

## User Stories

1. As a 教师, I want 教师看板显示“待核实学习记录”, so that 我能理解这是需要我确认的学生 AI 学习过程。
2. As a 教师, I want 教师看板避免“审计”, so that 我不会把教学正确性核实误解为后台系统审计。
3. As a 教师, I want 学情页面显示“本周核实覆盖”, so that 我知道这反映的是教学正确性核实覆盖情况。
4. As a 教师, I want 核实入口和按钮使用“学习记录核实”, so that 我可以按教学语境进入学生学习过程审阅。
5. As a 教师, I want 教师侧不出现 SFT、DPO、打标, so that 我只需要关注学生会话是否有误导和是否需要修订。
6. As a 教师, I want AI 预审先提示疑似误导内容, so that 我能更快定位需要重点查看的会话片段。
7. As a 教师, I want 在完整学生会话中看到标红片段, so that 我能结合上下文判断 AI 回答是否需要修订。
8. As a 教师, I want 对无问题会话点击“确认无误”, so that 该学习记录可以进入后续教学数据导出流程。
9. As a 教师, I want 直接在聊天气泡里修订回答, so that 我不需要跳转到单独表单完成教学纠正。
10. As a 学生, I want 看到教师修订提示, so that 我知道某条回答已经由教师修订并可按修订版本学习。
11. As an 管理员, I want 侧边栏学校管理分组里有“用户管理”, so that 我能从学校账号视角管理用户而不是进入权限配置语境。
12. As an 管理员, I want 用户管理页展示账号、姓名、角色、状态和最近活动, so that 我能快速掌握账号现状。
13. As an 管理员, I want 用户管理页提供 CSV 导入入口, so that 我可以批量导入学校账号。
14. As an 管理员, I want 用户管理页支持按角色筛选, so that 我能分别查看学生、教师和管理员账号。
15. As an 管理员, I want 用户管理页支持按状态筛选, so that 我能快速定位启用或停用账号。
16. As an 管理员, I want 用户管理页支持按姓名或账号全文搜索, so that 我能快速找到指定用户。
17. As an 管理员, I want 用户管理页显示教师负责班级数量, so that 我能判断教师班级归属是否完整。
18. As an 管理员, I want 用户管理页显示学生所在班级或未分配状态, so that 我能发现尚未归班的学生。
19. As an 管理员, I want 班级页展示每个班级的教师和学生成员概览, so that 我能在班级上下文中理解成员结构。
20. As an 管理员, I want 在班级页打开“成员分配”弹窗, so that 我不必跳转到单独班级详情页也能维护成员。
21. As an 管理员, I want 在成员分配弹窗中查看当前教师和学生成员, so that 我能在编辑前确认现状。
22. As an 管理员, I want 在成员分配弹窗中按角色添加账号, so that 我能把教师或学生加入当前班级。
23. As an 管理员, I want 在成员分配弹窗中移除已有成员, so that 我能修正错误班级归属。
24. As an 管理员, I want 同一个教师可以加入多个班级, so that 教师负责多个班级的真实教学关系可以被表达。
25. As an 管理员, I want 同一个学生只能属于一个班级, so that 学情统计和教学正确性核实范围保持清晰。
26. As an 管理员, I want 把学生加入新班级时自动迁班, so that 我不需要先去旧班级手动移除该学生。
27. As an 管理员, I want 自动迁班前看到“学生将从原班级迁入当前班级”, so that 我明确知道这次操作会改变学生原班级归属。
28. As an 管理员, I want 可以移除班级最后一个教师但看到风险提示, so that 我理解该班级可能暂时没有负责教师。
29. As an 管理员, I want AI 运维区域继续使用 Provider、MCP、SFT、DPO 等专业术语, so that AI Native 后台不被弱化成普通设置页。
30. As a 开发者, I want 学生单班规则由数据库硬约束防复发, so that 应用层遗漏自动迁班时也不会产生重复 student 班级关系。
31. As a 开发者, I want 保持 lint、typecheck、build 通过, so that 语境对齐不会破坏已落地的角色工作台。

## Implementation Decisions

- 教师侧内部命名可以暂时保留 audit，但所有教师可见文案必须使用“学习记录核实 / 待核实学习记录 / 教学正确性核实”等语义。
- 教师侧不暴露 SFT、DPO、打标；这些术语只允许出现在管理员 AI 运维和教学数据导出语境中。
- 管理员入口命名为“用户管理”，不使用“用户权限”，因为本阶段不做授权/RBAC 配置，角色权限在账号创建时已经确定。
- 用户管理 MVP 包含账号可视化、CSV 导入入口、角色/状态/活动展示、班级归属摘要、角色筛选、状态筛选和姓名/账号全文搜索。
- 班级成员管理放在班级页弹窗内完成，不新增独立班级详情页作为 MVP 必需路径。
- 成员分配弹窗必须支持查看当前成员、按角色添加账号、移除已有成员，并始终保持在当前班级上下文中。
- 教师账号支持多班级归属；学生账号只允许单班级归属。
- 学生加入新班级时执行自动迁班：系统移除学生原有班级关系，再加入当前班级，并在弹窗中提示迁班影响。
- 移除最后一个教师允许执行，但必须提示“移除后该班级暂无负责教师”，因为教师看板和教学正确性核实范围会受影响。
- 学生单班规则必须有数据库硬约束或等价强约束；已选择以只作用于 student 成员关系的唯一索引防止重复。
- 不重做 RLS，不改变 profiles、classes、class_memberships 的基本职责边界。
- 所有改动保持当前 Next.js App Router、server actions、Supabase 数据访问和现有组件体系风格。

## Testing Decisions

- 文案测试以外部可见行为为准：教师侧页面、侧边栏、按钮、空状态、错误状态不应出现“审计/SFT/DPO/打标”。
- 管理员 AI 运维和教学数据导出仍可出现 SFT/DPO/Provider/MCP，测试不应把管理员专业术语误判为失败。
- 用户管理页需要覆盖空数据、有用户、筛选、搜索、CSV 导入入口和班级归属摘要的渲染路径。
- 班级页需要覆盖无成员、有成员、打开成员分配弹窗、添加教师、添加学生、移除成员的外部行为。
- 自动迁班需要验证学生加入新班级后旧班级关系被移除，且教师多班不受影响。
- 移除最后一个教师需要验证风险提示出现，但操作不被硬性禁止。
- 数据库约束需要验证重复 student 成员关系被拒绝，重复 teacher 多班关系不被拒绝。
- 本轮完成前至少运行 lint、TypeScript typecheck 和 production build。
- 如果新增数据整理函数或成员分配 server action，应优先测试其外部输入输出；页面组合与文案改动以构建和关键路径渲染检查为主。

## Acceptance Criteria

- [ ] 教师侧可见中文文案不再出现“审计”“SFT”“DPO”“打标”。
- [ ] 教师侧使用“学习记录核实”“待核实学习记录”“教学正确性核实”“本周核实覆盖”等领域语言。
- [ ] 管理员侧边栏学校管理分组包含“用户管理”，不出现“用户权限”。
- [ ] 用户管理页展示账号、姓名、角色、状态、最近活动和班级归属摘要。
- [ ] 用户管理页提供 CSV 导入入口。
- [ ] 用户管理页支持按角色筛选、按状态筛选、按姓名或账号全文搜索。
- [ ] 班级页展示教师/学生成员概览。
- [ ] 班级页提供“成员分配”弹窗，而不是强制进入单独班级详情页。
- [ ] 成员分配弹窗支持查看当前成员、添加教师、添加学生、移除成员。
- [ ] 教师可以归属多个班级。
- [ ] 学生只能归属一个班级。
- [ ] 将学生加入新班级时自动迁班，并提示“学生将从原班级迁入当前班级”。
- [ ] 移除最后一个教师时显示“移除后该班级暂无负责教师”风险提示。
- [ ] 数据库层存在防止重复 student 班级关系的硬约束或等价唯一索引。
- [ ] 管理员 AI 运维与教学数据导出仍可显示 SFT/DPO/Provider/MCP 专业术语。
- [ ] lint 通过。
- [ ] TypeScript typecheck 通过。
- [ ] production build 通过。

## Out of Scope

- 不实现复杂权限矩阵、细粒度 RBAC、授权审批流或在线授权配置。
- 不在本阶段提供在线改角色或停用账号操作。
- 不重构 Supabase RLS。
- 不改变 audit_records、export_batches 等后台数据命名。
- 不重做学生项目、会话、挑战主路径。
- 不重做教师问答能力。
- 不新增真实浏览器端登录 E2E 作为本 PRD 的必需交付。

## Further Notes

本 PRD 的核心是把已落地 UIUX 主线继续收口到 `CONTEXT.md` 的领域语言：教师体验必须面向教学正确性核实，管理员体验必须同时保留学校管理和 AI Native 运维能力。学生单班数据库硬约束已经作为后续数据库操作落地，但应用层自动迁班和成员分配弹窗仍需要在 UI/数据层保持一致。
