# 模块 D：结果格式与结果检查

## 目标

保证模型输出必须经过 schema 校验和证据检查，才能保存为正式结果对象。

## 交付内容

- v1 三类结果对象 schema
- FLQC 校验节点
- 校验失败时的降级、拒绝保存和错误码
- 结果状态流转：`draft -> ready -> exported`

## 验收

1. 缺少 `evidence_refs` 的结果不得保存。
2. 字段不符合 schema 的结果会报错或退回草稿。
3. v2 结果对象不会作为 v1 默认输出。
