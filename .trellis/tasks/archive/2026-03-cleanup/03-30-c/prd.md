# 模块 C：本地检索与上下文模式

## 目标

实现离线可用的本地资料检索链路，让生成模块只拿到必要上下文。

## 交付内容

- ChromaDB 初始化与资料入库
- 文本切块、Embedding、按项目和内容类型过滤
- `lite` / `full` 两种上下文模式
- 检索失败时的回退策略

## 验收

1. 资料可按项目写入和检索。
2. `lite` 模式只返回轻量信息，`full` 模式返回正文片段和注释。
3. 检索失败时能写入错误码并降级，而不是直接崩溃。
