# 《文韵智途》毕业设计答辩 — Slide Deck 大纲与讲稿

> 时长：12-15 分钟汇报 + 5 分钟提问
> 听众：计算机专业答辩委员会
> 核心 takeaway：这不是一个聊天工具，而是一个把 AI 纳入教学闭环的完整系统

---

## Slide 1 | 封面

**标题**：文韵智途——面向古诗文学习与教学的 AI 教学工作台

**副标题**：毕业设计答辩

**信息**：学生姓名 / 学号 / 专业 / 指导教师

**讲稿**（30秒）：
> 各位老师好，我的毕业设计题目是《文韵智途——面向古诗文学习与教学的 AI 教学工作台的设计与实现》。这个项目关注的核心问题是：AI 如何在古诗文教学中被合理组织、可信使用和持续治理。

---

## Slide 2 | 问题提出

**标题**：古诗文 + AI = 四个未解决的问题

**内容**（4 条，每条一行）：
1. 学生学习过程碎片化——提问多但不成体系
2. 学习结果难以确认——聊天 ≠ 掌握
3. AI 回答可信性不足——流畅 ≠ 正确
4. 教学数据难以高质量沉淀——原始对话 ≠ 训练样本

**讲稿**（60秒）：
> 古诗文教学有一个特点：学生的困惑是持续的、分层的，但传统课堂和现有 AI 工具都只能做到"一问一答"。我梳理了四个核心问题：第一，学生提问很多但不成体系；第二，聊天能看热闹但无法判断是否真正掌握；第三，AI 回答流畅但不一定教学正确；第四，如果把原始对话直接当训练数据，质量无法保证。这四个问题就是我这个系统要解决的。

---

## Slide 3 | 解决思路：一条闭环

**标题**：核心设计——学生提问到教学数据的六步闭环

**内容**：流程图（文字版）
```
学生提问 → 篇目识别 + 项目归属 → 布鲁姆认知路径
    → 挑战确认认知层级 → 教师学习记录核实
    → SFT/DPO 样本物化 → 管理员导出
```

**讲稿**（60秒）：
> 我的解决思路是一条完整的闭环。学生提问后，系统自动识别篇目并归入项目；通过挑战而不是聊天来确认认知层级；教师对 AI 回答做教学正确性核实；只有教师处理过的内容才能进入可导出的训练样本。这条闭环把"学习过程、学习确认、教学把关、数据治理"串成了一个整体。

---

## Slide 4 | 三类角色

**标题**：三类角色，各司其职

**内容**：三列对比

| 学生 | 教师 | 管理员 |
|------|------|--------|
| 学习主体 | 质量把关者 | 平台治理者 |
| 提问 + 项目 + 挑战 | 看板 + 问答 + 核实 | 用户 + 能力 + 导出 |
| 看到 AI 回答 | 看到完整学习过程 | 只看已核实样本 |

**讲稿**（40秒）：
> 系统面向三类角色。学生是学习主体，围绕篇目持续提问和挑战；教师是质量把关者，在完整会话语境中核实 AI 回答；管理员是平台治理者，负责能力配置和数据导出。三者职责严格分离，管理员永远不看学生原始对话。

---

## Slide 5 | 技术架构

**标题**：五层全栈架构

**内容**：架构图（简化版）
```
表现层：Next.js 16 App Router + shadcn/ui + Tailwind v4
业务层：Route Handlers + Server Actions
AI 层：Vercel AI SDK v6 + MCP 工具 + 8 种 Capability
数据层：Supabase (PostgreSQL 17) + pgvector + RLS + 触发器
治理层：日志 + 导出批次 + 事件观测
```

**讲稿**（50秒）：
> 技术上采用五层架构。前端是 Next.js 16 的 App Router，后端直接用 Supabase 作为唯一数据真源。AI 能力层通过 Vercel AI SDK 抽象了 8 种场景能力，管理员可以在后台一键切换 Provider 和模型。权限层的核心是 PostgreSQL 的行级安全策略加契约式触发器，把产品规则固化在数据库层。

---

## Slide 6 | 学生端：项目化学习

**标题**：学生侧——从零散提问到项目化学习

**内容**：
- 提问即入：不要求先建项目，系统自动归属
- 篇目项目：同一学生同一篇目只有一个项目
- 日常会话归档：无法归属时的兜底容器
- 项目不可删除（数据库触发器强制）

**配图建议**：学生看板卡片墙截图

**讲稿**（50秒）：
> 学生进入系统后可以直接提问，不需要先建项目。系统会自动识别篇目并归入对应项目。同一学生对同一篇目永远只有一个项目，学习过程持续沉淀。如果无法识别篇目，就进入日常会话归档。项目不可删除，这是通过数据库触发器强制的。

---

## Slide 7 | 学生端：挑战确认

**标题**：挑战机制——会话是过程，挑战是确认

**内容**：
- 布鲁姆六层：记忆 → 理解 → 应用 → 分析 → 评价 → 创造
- 三段式结构化题目：证据定位 → 层级任务 → 自我校验
- 输入最小化：只用篇目 + 学生问题 + 目标层级，不输入 AI 回答
- 层级解锁：必须逐层通过，不能跳级

**讲稿**（60秒）：
> 这是系统最有特色的设计之一。我们不用聊天来判断学生是否掌握，而是用挑战。挑战基于布鲁姆认知层次，从记忆到创造六层逐级确认。每个挑战是三段式结构化题目：先定位证据，再完成层级任务，最后自我校验。生成挑战时只输入篇目、学生问题和目标层级，不输入任何 AI 回答或教师修订，避免偏置。

---

## Slide 8 | 教师端：学习记录核实

**标题**：教师侧——在完整语境中核实，不是批改队列

**内容**：
- 四级导航：班级 → 学生 → 项目 → 会话
- AI 预审：两阶段策略（全会话 + 单条补审），标红疑似问题段
- 修订回答：直接编辑气泡，立即同步学生侧
- 会话级最终提交：一次性确认整条会话，触发样本物化

**讲稿**（60秒）：
> 教师端的核心是学习记录核实。教师不是面对一条条孤立回答，而是在班级、学生、项目、会话的完整语境中处理。系统先用 AI 预审标红疑似问题段，教师再做最终判断。修订回答会立即同步给学生。最关键的是"会话级最终提交"——教师对整条会话一次性确认，这时才会生成 SFT 和 DPO 样本。

---

## Slide 9 | 数据治理

**标题**：数据治理——只有教师处理过的内容才能成为样本

**内容**：

| 回答状态 | SFT | DPO |
|---------|-----|-----|
| 未修订 + 最终提交 | ✅ AI 原回答 | ❌ |
| 修订过 + 最终提交 | ✅ 教师修订版 | ✅ chosen=修订版, rejected=原回答 |
| 仅保存修订 | ❌ | ❌ |

- 导出格式：JSONL（SFT / DPO / metadata）
- 按班级、教师、篇目、时间筛选

**讲稿**（50秒）：
> 数据治理是这个系统区别于普通教育 AI 产品的关键。未经教师处理的内容不能进入样本。只有会话级最终提交才能物化样本。未修订的回答进入 SFT，修订过的回答同时进入 SFT 和 DPO。管理员可以按条件筛选导出 JSONL。

---

## Slide 10 | 数据库设计亮点

**标题**：规则下沉——产品规则固化在数据库层

**内容**（4 条关键约束）：
1. `class_memberships_one_student_class_idx`：学生单班（部分唯一索引）
2. `text_projects_prevent_delete`：项目不可删除（触发器）
3. `validate_conversation_message_contract`：finalized 会话阻止学生续问
4. `validate_audit_record_contract`：日常会话归档不进入审阅

**数据**：18 张表 / 7 种枚举 / ~45 条 RLS / 31 条触发器 / 15 个 RPC

**讲稿**（50秒）：
> 数据库设计的核心理念是"规则下沉"。所有关键产品规则都通过 RLS 策略和触发器固化在数据库层。比如学生单班用部分唯一索引强制，项目不可删除用触发器阻止，finalized 会话阻止学生续问也是触发器实现。即便应用层有 bug，数据库层也会拦截。整个系统有 18 张表、约 45 条 RLS 策略和 31 条触发器。

---

## Slide 11 | AI 能力路由

**标题**：三层绑定——管理员一键切换 AI 能力

**内容**：
```
Provider (OpenAI/Gateway/Anthropic/...)
    ↓
Model Tier (flash / advanced)
    ↓
Scenario (student_chat / practice_generation / audit_assist / ...)
```

- 8 种 capability 场景
- 管理员后台配置，零代码切换
- MCP 工具按角色隔离

**讲稿**（40秒）：
> AI 能力路由是三层绑定。最底层是 Provider，中间是模型等级（flash 或 advanced），最上层是场景。管理员在后台配置后，应用层通过一个 `getCapability()` 函数就能拿到对应的模型。这样学生侧可以用便宜模型，教师预审可以用更强的模型，切换不需要改代码。

---

## Slide 12 | 系统验证

**标题**：测试与验证

**内容**：
- 39 个单元测试全部通过（挑战进度、预审归一化、SFT/DPO 构造等）
- 端到端流水线测试：学生提问 → AI 回答 → 教师修订 → 最终提交 → 导出
- 云端 Supabase 实例端到端验证
- Supabase Advisor：性能 + 安全 lint 全部处理

**讲稿**（30秒）：
> 系统通过三层测试验证：39 个单元测试覆盖核心逻辑，端到端脚本模拟完整链路，所有功能在云端 Supabase 实例上完成验证。Supabase 的性能和安全 advisor 检测出的问题也已全部处理。

---

## Slide 13 | 创新点

**标题**：五个创新点

1. 古诗文学习从零散问答 → 项目化学习单元
2. 认知评价从会话推断 → 挑战确认（过程与确认分离）
3. 教师核实纳入 AI 学习闭环主流程
4. AI 能力治理从代码中解耦（三层绑定）
5. 产品规则通过 RLS + 触发器固化到数据库层（规则下沉）

**讲稿**（40秒）：
> 总结五个创新点。第一，把学习从零散问答提升为项目化单元；第二，用挑战替代会话推断来确认认知层级；第三，把教师核实纳入 AI 闭环主流程；第四，AI 能力治理从代码中解耦；第五，产品规则通过 RLS 和触发器固化到数据库层。

---

## Slide 14 | 总结与展望

**标题**：总结与展望

**总结一句话**：
> 文韵智途解决的不是"AI 能不能回答古诗文问题"，而是"AI 如何在古诗文教学中被合理组织、可信使用、持续治理"。

**未来方向**（3 条）：
- 面向古诗文的专用 RAG（字义、典故、意象索引）
- 教师端协作审阅与跨教师争议复核
- 基于 Storage 的大批次导出 + 审计链

**讲稿**（40秒）：
> 总的来说，这个系统从学生学习、教师核实和管理员治理三个维度，构建了一个兼顾学习体验、教学可信性和数据治理能力的 AI 教学平台。未来可以继续扩展古诗文专用检索、教师协作审阅和更完善的导出治理。我的汇报到这里，谢谢各位老师。

---

## Slide 15 | 致谢 + Q&A

**标题**：谢谢各位老师，欢迎批评指正

---

## Backup Slides（Q&A 准备）

### B1 | 为什么不用 auth.uid()？

系统使用学校账号登录（8 位学号/工号），不走 Supabase Auth 的邮箱/OAuth 流程。通过自定义 `current_app_user_id()` + `has_valid_app_session_signature()` 函数桥把应用层会话映射到 RLS。

### B2 | 挑战评估的准确性如何保证？

三段式结构化题目强制学生给出证据 + 任务 + 校验；评估提示词明确"三步都成立且目标层级能力明确时才通过"；结果只有通过/未通过，不做模糊评分。

### B3 | 如果 AI 预审漏判怎么办？

两阶段策略：全会话预审 + 单条补审。`normalizePreReviewResults()` 检查每条 AI 回答是否有有效结果，缺失的自动补审。最终教师仍是判断主体，预审只是定位辅助。

### B4 | 日常会话归档为什么不进入教师核实？

CONTEXT.md 明确规定：无法可靠归属篇目的会话不进入教师核实、不生成布鲁姆认知路径、不生成挑战。`validate_audit_record_contract` 触发器在数据库层强制 `project_id IS NULL` 的会话不能写入 `audit_records`。

### B5 | 数据库优化做了什么？

三条 migration：
1. 16 条索引（教师审阅复合偏索引 + FK 覆盖）+ 6 条冗余 RLS 清理
2. 26 个 SECURITY DEFINER 函数权限收紧
3. finalized 会话阻止学生续问（修复 CONTEXT.md 规则缺口）

### B6 | 系统能支撑多大规模？

当前设计面向单校场景（1-5 个班级、50-200 学生）。索引设计已覆盖教师审阅的 P95 查询路径。向量检索使用 HNSW，在小图场景下 recall 稳定。大规模扩展需要物化视图 + JSONL 外部化到 Storage。

---

## 讲稿时间分配

| Slide | 内容 | 时间 |
|-------|------|------|
| 1 | 封面 | 0:30 |
| 2 | 问题提出 | 1:00 |
| 3 | 解决思路 | 1:00 |
| 4 | 三类角色 | 0:40 |
| 5 | 技术架构 | 0:50 |
| 6 | 学生端：项目化 | 0:50 |
| 7 | 学生端：挑战 | 1:00 |
| 8 | 教师端：核实 | 1:00 |
| 9 | 数据治理 | 0:50 |
| 10 | 数据库亮点 | 0:50 |
| 11 | AI 能力路由 | 0:40 |
| 12 | 系统验证 | 0:30 |
| 13 | 创新点 | 0:40 |
| 14 | 总结展望 | 0:40 |
| 15 | 致谢 | 0:10 |
| **合计** | | **~11:00** |

留 1-2 分钟余量用于现场 demo 或节奏调整。

---

## 制作建议

1. **配图优先级**：Slide 3（闭环图）> Slide 5（架构图）> Slide 7（布鲁姆阶梯）> Slide 6/8（截图）
2. **每页最多 6 个元素**（标题 + 4-5 条要点或一张图）
3. **标题用 claim 句式**（"学生单班用部分唯一索引强制"而不是"班级管理"）
4. **不要把论文搬上 PPT**——PPT 是广告，论文是正文
5. **Demo 准备**：服务已在 localhost:3000 运行，可以现场演示学生提问 → 项目归属 → 挑战生成的完整流程
