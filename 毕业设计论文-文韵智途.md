# 文韵智途：面向古诗文教学的 AI 教学工作台

## 毕业设计（论文）

**题目**：文韵智途——面向古诗文学习与教学的 AI 教学工作台的设计与实现

**专业**：计算机科学与技术

**指导教师**：（由学生填写）

**学生姓名 / 学号**：（由学生填写）

**完成日期**：2026 年 5 月

---

## 摘要

古诗文教学具有知识密集、理解分层、表达开放等特点。传统课堂主要依赖教师一问一答，而学生课后自主学习的过程往往分散在零散提问与搜索之中，难以沉淀为可持续、可评价、可反馈的学习单元。近年来生成式大语言模型为文言文教学带来了新的答疑与讲解能力，但也带来了三类新的问题：AI 回答的可信性无法天然保证；学生学习过程无法被教师有效观察与干预；AI 产出的内容如果未经把关就进入教学数据资产，会对后续模型训练与教学研究造成噪声。

针对上述问题，本课题以"古诗文教学闭环"为出发点，设计并实现了一个面向学生、教师、管理员三类角色的 AI 教学工作台——文韵智途（Classical Chinese Workbench）。系统围绕"学生提问—项目沉淀—挑战确认—教师核实—管理员治理"这条主线，将会话组织为围绕篇目的项目化学习单元；通过布鲁姆认知路径与挑战机制把"学习过程"与"学习结果确认"区分开；通过教师的学习记录核实把 AI 回答纳入可控的教学质量把关流程；通过 AI Native 后台把账号、班级、模型、日志与 SFT/DPO 导出纳入统一治理。

系统采用 Next.js 16 App Router 全栈架构，使用 Supabase 作为唯一业务真源，集成 pgvector + HNSW 索引实现会话级 RAG 检索；AI 能力层使用 Vercel AI SDK，抽象出学生会话、教师问答、布鲁姆分类、篇目归属、挑战生成与评估、教师预审、Embedding 等八类场景能力，通过管理员后台的 Provider、Capability、Tier、Scenario 分层绑定完成运行时解析；权限层通过 PostgreSQL 行级安全（RLS）策略 + 契约式触发器把产品规则固化在数据库层，避免应用代码绕过。数据治理层实现了教师会话级最终提交、SFT/DPO 样本物化与按批次可筛选的 JSONL 导出。

系统已完成学生、教师、管理员三端的核心功能闭环，在云端 Supabase 实例上完成端到端验证。本文还在现有实现基础上进一步提出了数据库侧的若干优化建议（包括针对高频筛选的复合索引、事件表去冗、向量检索配置与样本索引分区等），以支撑后续在更大班级规模、更多篇目和更长运行周期下的持续演进。

**关键词**：古诗文教学；AI 教学工作台；布鲁姆认知层次；检索增强生成；教学数据治理；Supabase；行级安全

---

## Abstract

Classical Chinese literature teaching is knowledge-dense, hierarchically structured, and highly expressive. Traditional classrooms rely mostly on real-time teacher–student dialogue, while students' out-of-class self-study remains scattered across ad-hoc questions and isolated searches, rarely consolidating into a durable, assessable, and reviewable learning unit. Recent large language models have introduced new answering and explaining capabilities into Classical Chinese learning, but also introduced three fresh problems: the pedagogical trustworthiness of AI answers is not guaranteed; students' learning processes are not observable by teachers; and if AI outputs enter the dataset pipeline without review, they degrade the quality of downstream training corpora.

To address these gaps, this thesis designs and implements an AI teaching workbench for Classical Chinese education, named **Classical Chinese Workbench (CCW, 文韵智途)**, targeting three roles: student, teacher, and administrator. The system is built around the closed loop of *asking → project consolidation → challenge confirmation → teacher verification → administrator governance*. Student conversations are organized into piece-centric learning projects; Bloom's cognitive pathway plus a challenge mechanism separate "learning process" from "confirmed learning outcome"; a teacher review workflow brings AI answers under controlled pedagogical quality gating; and an AI-Native admin console unifies accounts, classes, AI providers, logs, and SFT/DPO dataset export.

The system is implemented on Next.js 16 App Router with Supabase as the single source of truth, using pgvector + HNSW indexing for conversation-scoped RAG. The AI capability layer is built on Vercel AI SDK and abstracts eight capability scenarios, resolved at runtime via Provider / Capability / Tier / Scenario bindings in the admin console. Row-level security (RLS) policies and contract-enforcing triggers push product invariants down to the database layer, preventing application-level bypass. The data-governance layer implements conversation-level teacher finalization, SFT/DPO sample materialization, and batched, filter-ready JSONL export.

Full student, teacher, and administrator workflows have been implemented and validated end-to-end against a production Supabase instance. This thesis also proposes further database-side optimizations over the current implementation — covering composite indexes for high-frequency filters, event-table deduplication, vector search tuning, and audit sample partitioning — to support larger classrooms, more pieces, and longer operational windows.

**Keywords**: Classical Chinese education; AI teaching workbench; Bloom's taxonomy; Retrieval-Augmented Generation; teaching data governance; Supabase; Row-Level Security

---

## 目录

- 第一章 绪论
- 第二章 相关技术与文献综述
- 第三章 需求分析
- 第四章 系统总体设计
- 第五章 数据库设计
- 第六章 学生端设计与实现
- 第七章 教师端设计与实现
- 第八章 管理员端与 AI 能力治理
- 第九章 数据治理与 SFT/DPO 导出
- 第十章 系统测试与部署
- 第十一章 数据库与系统进一步优化建议
- 第十二章 总结与展望
- 参考文献
- 致谢

---

## 第一章 绪论

### 1.1 研究背景

古诗文是中华优秀传统文化的重要载体，也是中学语文教学中最具挑战性的一类文本。与现代白话文相比，古诗文具有语法结构精简、用字考究、意象叠合、情感含蓄、表达多义等特点。学生在学习过程中普遍会遇到字词难辨、句读不明、意象理解偏差、情感体会不到位、表达手法难以迁移等一系列认知障碍。与此同时，教学一线也面临几类长期存在的困难：课堂时间有限，教师难以就每一个学生的疑问给予充分解释；课后学生自主学习的过程分散在家庭作业、搜索引擎、各类参考资料甚至同伴讨论之间，教师几乎无法持续跟踪其真实进展。

近年来，以大语言模型（Large Language Model, LLM）为代表的生成式人工智能技术进入教育领域，为古诗文教学提供了新的答疑与讲解可能。学生可以随时就某一句、某一字甚至某种情感向 AI 提问，AI 也可以以接近教师的口吻组织回答。这一方面确实降低了古诗文学习的"起点门槛"，另一方面也把三类新的问题摆到了教学一线面前：

- **可信性问题**：LLM 的回答在语言流畅度上往往高于教学严谨度，模型可能出现误解字词、牵强解读意象、弱化情感层次、甚至"自圆其说"的情况；
- **过程不可见**：如果 AI 只是在学生浏览器里自说自话，教师既看不到提问过程，也看不到回答内容，课堂外的学习路径完全脱离教师视野；
- **数据治理问题**：如果把 AI 原始对话直接用作后续监督微调（SFT）或偏好优化（DPO）的训练数据，未经过教学把关的内容会把错误、偏差、风格噪声一并带入模型迭代链路，造成数据资产质量下滑。

因此，一个面向实际教学的 AI 古诗文学习系统，不能只解决"AI 能不能回答古诗文问题"，还必须回答"AI 的回答如何被组织、如何被核实、如何被治理"。这正是本课题关注的问题起点。

### 1.2 研究意义

本课题的研究意义可以从三个层面来描述。

**教学层面**。本课题将学生对古诗文的零散提问上升为围绕具体篇目的项目化学习单元，让学习过程不再停留在碎片化会话。系统通过布鲁姆认知路径刻画"学生在某个问题上想达到的认知层次"，通过挑战机制对"学生在该项目上已经确认达到的认知层次"做分层确认，使教师、学生都能对"学到哪一层"这个问题有一个清晰、共享的判断口径。

**技术层面**。本课题把 LLM、RAG、MCP（Model Context Protocol）工具编排、教学语义的业务规则、数据治理、行级安全控制等多项技术，合并到一套面向教育业务的产品里。系统坚持"数据库真源 + 应用层编排"的原则：把涉及身份、可见性、软删除、会话容器归属、样本产出等关键规则固化在 Supabase/PostgreSQL 的 RLS 策略与契约式触发器里，而不是只写在应用层，避免因为任何一条应用代码漏判就绕过产品边界。

**数据资产层面**。本课题从一开始就把"高质量教学数据沉淀"作为系统级目标，而不是运行一段时间之后再补建导出能力。教师对 AI 回答的确认无误与修订回答不是附属动作，而是直接决定哪些会话内容会以 SFT/DPO 的形式被物化为样本，并带上审阅元数据用于可追溯的批次导出。这条路径使系统不止服务当前教学，也为后续的教学模型优化与学情研究提供了可持续的数据基础。

### 1.3 国内外研究现状

目前国内外与"古诗文 + AI + 教学数据治理"直接相关的公开研究较少，但可以归纳为四条相关技术线：

1. **智能辅导系统（Intelligent Tutoring System, ITS）**。传统 ITS 基于知识追踪（Knowledge Tracing）与贝叶斯网络建模学生认知水平，偏重于结构化题库（数学、编程等），并不直接适用于开放表达类的古诗文学习。
2. **大语言模型辅助教学**。ChatGPT、Claude、通义千问等模型具备良好的中文古诗文问答能力，但使用场景大多是单轮对话，缺少面向班级、面向教师、面向数据治理的工程化组织。
3. **检索增强生成（Retrieval-Augmented Generation, RAG）**。pgvector、HNSW、Chroma、Milvus 等技术方案成熟，常见的教学场景应用是"把某一本教材或某一个文档库接入问答"，但较少讨论"如何在学生会话级别做向量作用域隔离"以及"如何与教师审阅流程结合"。
4. **教学数据治理与模型后训练**。监督微调（SFT）、直接偏好优化（Direct Preference Optimization, DPO）等技术在模型迭代中已非常常见，但教学场景下如何保证训练样本来自"教师确认过的真实教学过程"，而不是来自未审的原始对话，仍缺乏成熟的工程范式。

本课题结合以上四条技术线，围绕古诗文教学这一具体场景，构建了一套相对完整的系统方案。

### 1.4 本文主要工作

本文的主要工作包括：

1. 在需求分析层面，梳理古诗文 AI 教学场景的问题结构，明确学生、教师、管理员三类角色的职责边界，确定"学生提问—项目沉淀—挑战确认—教师核实—管理员治理"的核心业务闭环；
2. 在系统设计层面，设计了一套面向 Next.js 16 App Router + Supabase 的全栈架构，定义了前端表现层、业务交互层、数据服务层、AI 能力层、治理运维层五层结构；
3. 在数据库设计层面，采用 PostgreSQL 17 + pgvector，完成 18 张业务表 + 7 种枚举类型 + 大量 RLS 策略和契约式触发器的实现；
4. 在应用实现层面，完成学生提问与项目归属、布鲁姆挑战、教师学习记录核实、教师 AI 预审、教师会话级最终提交、管理员用户/班级/Provider/MCP/Prompt/日志/导出等主要功能的开发；
5. 在数据治理层面，设计并实现 SFT/DPO 样本的物化流程与按条件筛选的批次 JSONL 导出；
6. 在优化层面，针对当前云端 Supabase 实例上观察到的若干实际问题，给出数据库索引、事件表结构、向量检索、审阅样本分区等方面的进一步优化建议。

### 1.5 论文结构

本文共十二章。第二章介绍相关技术与文献基础；第三章进行系统需求分析；第四章描述系统总体设计；第五章给出数据库设计；第六至第九章分别介绍学生端、教师端、管理员端与数据治理的实现；第十章介绍系统测试与部署；第十一章针对数据库与系统提出进一步的优化建议；第十二章对全文工作进行总结并对未来方向进行展望。

---

## 第二章 相关技术与文献综述

本章从理论基础、前端技术、后端与数据技术、AI 技术、安全与治理技术五个维度梳理本课题依赖的关键技术。

### 2.1 教学理论基础：布鲁姆认知层次

Benjamin Bloom 于 1956 年提出的教育目标分类学（Taxonomy of Educational Objectives），经 Anderson 与 Krathwohl 于 2001 年修订后，形成被广泛采用的认知层次框架。该框架将认知目标由低到高划分为六个层级：**记忆（Remember）、理解（Understand）、应用（Apply）、分析（Analyze）、评价（Evaluate）、创造（Create）**。这一框架在题目设计、教学评价与学习分析等场景中被大量使用。

本课题把布鲁姆认知层次嵌入两个位置：一是对每个学生问题做**布鲁姆认知路径**判断，判定该问题要被真正学懂所需要经过的认知最高充分层次；二是对每个项目做**挑战**，通过分层任务真实确认学生对某个篇目已经达到的认知层级。这样做的重要原则是："会话体现过程，挑战体现确认"——会话级的认知路径只作为学习过程的表征，不直接用作项目认知结果的判定依据。

### 2.2 前端技术栈

**Next.js 16 App Router**。本项目使用 Next.js 16（基于 React 19）的 App Router 架构。App Router 原生支持 React Server Components、流式渲染、嵌套布局、Route Handler 以及 Server Actions。相比传统的 Pages Router，App Router 把"数据获取、渲染、交互"合并在同一文件系统语义下，使页面级的数据权限判断可以在服务端一次性完成，再以 props 形式透传到客户端组件。

**React 19 并发特性**。React 19 的 `useOptimistic`、`useFormStatus`、`useActionState` 等 Hook 与 Server Actions 深度配合，适合表单密集场景（教师审阅、管理员配置）的乐观 UI。

**shadcn 风格组件 + Base UI + Tailwind CSS v4**。项目使用 shadcn CLI 与 Tailwind v4 建立可编译、可定制的 UI 组件形态，底层交互原语主要来自 `@base-ui/react`，图标使用 `lucide-react`。在此基础上，系统封装了 `Sidebar`、`Dialog`、`ProjectCard`、`BloomBadge`、`ChallengeClient` 等业务组件。

**@ai-sdk/react**。Vercel 的 AI SDK 客户端版本提供了 `useChat` 等 Hook，以及 `Message`、`UIMessage`、`UIMessagePart` 的类型抽象，用于处理 AI 输出的流式展示与工具调用渲染。

### 2.3 后端与数据技术

**Supabase + PostgreSQL 17**。Supabase 作为开箱即用的 Postgres + Auth + Storage + Realtime + Edge Functions 的托管平台，提供了本课题所需的全部后端能力。本项目以云端 Supabase 为唯一业务数据真源，本地只保留 `web/supabase/migrations/` 作为 schema-as-code 记录。

**pgvector + HNSW**。pgvector 扩展在 Postgres 上原生提供 `vector` 类型与 `<=>`、`<#>`、`<->` 三种距离算子。本项目当前对 `document_chunks.embedding` 使用 `vector(768)` + `vector_cosine_ops` + HNSW 索引，用于会话级 RAG 的近邻检索。代码层在附件入库与会话检索时显式请求 768 维 embedding，同时保留通过能力路由切换 embedding Provider / model 的空间。与 IVFFlat 相比，HNSW 在相对较小的嵌入量下构建速度更快、查询质量更稳定，适合教育场景这种"单会话、少量文档"的高并发小图检索。

**Row-Level Security (RLS)**。PostgreSQL 的行级安全允许在表级别定义一组 `SELECT / INSERT / UPDATE / DELETE` 的条件策略。本项目把"学生只看自己、教师只看自己班级、管理员看全部"的权限规则全部写成 RLS，并通过自定义函数 `current_app_user_id()`、`current_profile_role()`、`is_admin()`、`teacher_can_access_class()` 做策略复用。这样即便应用层出现逻辑漏洞，数据库层也会拦截越权查询。

**Realtime + 自定义会话签名**。Supabase Realtime 原本依赖浏览器端的 JWT，本项目使用自定义的学校账号登录体系，因此需要通过自定义 `has_valid_app_session_signature()` 函数把当前应用层会话签名暴露给数据库的 RLS 判断。

### 2.4 AI 能力技术

**Vercel AI SDK v6**。AI SDK 提供了 `generateText`、`streamText`、`generateObject`、`tool` 等统一接口，屏蔽了 OpenAI、Anthropic、Gateway 等 Provider 之间的差异。本项目通过 `@ai-sdk/openai`、`@ai-sdk/mcp`、`ai` 三个包完成 Provider、MCP 工具、能力路由的组合。

**Model Context Protocol (MCP)**。MCP 是 Anthropic 推出的一种"把工具 / 数据源通过统一协议暴露给 LLM 使用"的协议。本项目在 `mcp_servers` 表里定义了 MCP 服务器条目，包含 `enabled_tools`、`allowed_roles`（`{teacher, student}`）、`is_enabled`、`health_status` 等字段。运行时根据角色过滤可用 MCP 工具（见 `src/lib/mcp-runtime-core.ts`），教师与学生可以使用的工具集完全独立。

**Capability / Tier / Scenario 分层路由**。为了在管理员后台内一键切换不同 AI 场景所用的模型与 Provider，本项目设计了分层绑定：

- **Provider**（`provider_configs`）：云端、本地、Gateway、OpenAI、Anthropic、Ollama、Azure 等通用访问凭据；
- **Capability**（`provider_capabilities`）：把每个 Provider 提供的能力与具体 `model_id` 绑定，枚举取值为 `student_chat / teacher_chat / bloom_classification / project_classification / practice_generation / practice_evaluation / audit_assist / embedding`；
- **Tier / Scenario**（`model_tier_bindings` + `scenario_tier_bindings`）：把 "flash / advanced" 两个模型等级与 Provider + model_id 绑定，再把每个 capability 映射到一个等级。

这一结构允许在不改代码的前提下，把"学生侧使用 flash 模型"与"教师预审使用 advanced 模型"的策略切换为管理员后台一次配置即可生效。

**RAG 会话级作用域**。本项目的检索增强生成在三个维度上做作用域判断：当前学生 `owner_id`、当前对话 `conversation_id`、当前项目 `project_id` 或班级 `class_id`。函数 `match_conversation_document_chunks()`（见第 5 章）完成向量匹配后，再通过 RLS 策略自然过滤越权结果。

### 2.5 监督微调与偏好优化

**Supervised Fine-Tuning (SFT)**。SFT 通过 `{messages}` 形式的对话样本对基座模型进行指令微调，每个样本为一个或多个 `system / user / assistant` 消息序列。

**Direct Preference Optimization (DPO)**。DPO 是 Rafailov 等人在 2023 年提出的偏好优化方法，直接以 `{prompt, chosen, rejected}` 的形式使用偏好对样本，不再需要训练独立的奖励模型。本项目把"教师修订版 vs AI 原回答"自然映射为 `chosen vs rejected`，构成了 DPO 样本的直接来源。

### 2.6 小结

本课题的技术栈选择遵循"云端优先、服务端为主、能力路由可配"三条原则：云端优先使应用具备真实的多用户协同能力；服务端为主使权限与业务规则可以在 RLS 与服务端代码里统一落地；能力路由可配使同一套业务代码可以在不同 Provider、不同等级模型之间灵活切换，适应教学场景中"成本 / 效果 / 合规"三者之间的动态权衡。

---

## 第三章 需求分析

本章围绕古诗文 AI 教学场景的实际问题，从业务目标、用例、功能需求和非功能需求四个层面进行需求分析。

### 3.1 业务目标与核心问题

在系统设计启动前，本课题对古诗文 AI 教学面临的真实问题进行了系统性梳理，归纳为四类核心问题：

| 核心问题 | 具体表现 | 系统级解决方向 |
| --- | --- | --- |
| 学生学习过程碎片化 | 提问频繁但不成体系，同一篇目的理解演进无法沉淀 | 以篇目项目为核心沉淀学习过程，提问自动归属到项目 |
| 学习结果难以确认 | 会话可以看热闹，但无法判断学生是否真正掌握 | 用挑战机制替代对话推断，严格以"挑战通过"确认认知层级 |
| AI 回答可信性不足 | AI 回答流畅但未必教学正确 | 教师介入学习记录核实，执行确认无误或修订回答 |
| 教学数据难以高质量沉淀 | 直接导出原始对话引入噪声 | 只有教师处理后的回答才进入 SFT/DPO 样本 |

系统的总体业务目标可以概括为一句话：让古诗文学习过程可沉淀、让学习结果可确认、让 AI 回答可把关、让教学数据可治理。

### 3.2 用户角色与用例

系统面向三类角色，彼此职责清晰分离：

#### 3.2.1 学生角色

学生是学习主体。主要用例包括：

- **U1 登录**：使用 8 位学号（4 位入学年份 + 2 位班号 + 2 位流水号）登录；
- **U2 学习提问**：从全局空白入口直接提问古诗文问题，系统完成篇目识别与项目归属；
- **U3 在项目下继续学习**：围绕同一篇目持续提问，会话自动归属到该项目；
- **U4 查看日常会话归档**：查看未能归入篇目的兜底会话；
- **U5 发起挑战**：在项目层级上挑战目标布鲁姆层级，系统生成结构化题目；
- **U6 查看认知概览**：在学生看板上查看项目卡片墙、当前挑战层级、布鲁姆认知分布；
- **U7 接收教师修订提示**：当教师修订过某条 AI 回答时，学生侧显示修订提示。

#### 3.2.2 教师角色

教师是教学质量把关者。主要用例包括：

- **T1 登录**：使用 8 位工号（4 位入职年份 + 4 位流水号）登录；
- **T2 查看教师看板**：查看待核实压力、风险回答数、班级认知概览；
- **T3 使用教师问答**：借助 AI 与提示词模板完成备课、讲解设计、追问设计等工作；
- **T4 学习记录核实**：在班级 → 学生 → 项目 → 会话的四级导航下，对学生会话里每条 AI 回答做教学判断；
- **T5 AI 预审**：对整个会话调用预审能力，获得逐条回答的风险片段提示；
- **T6 修订回答**：直接编辑 AI 回答气泡生成草稿修订；
- **T7 会话级最终提交**：对整条会话一次性确认无误，触发 SFT/DPO 样本物化。

#### 3.2.3 管理员角色

管理员是平台治理者。主要用例包括：

- **A1 用户与班级管理**：CSV 导入账号、管理班级、编辑成员；
- **A2 Provider 管理**：新增 / 编辑 / 启停 AI Provider，配置密钥、健康检查；
- **A3 能力路由**：为每种 capability 绑定 Provider 与模型；
- **A4 提示词预设管理**：定义 Prompt 预设，区分目标角色（teacher/student）与发布状态；
- **A5 MCP 工具配置**：为教师端 / 学生端分别启停 MCP 工具；
- **A6 教学数据导出**：按时间、班级、教师、篇目等条件筛选可导出样本，生成 SFT / DPO / metadata 批次 JSONL；
- **A7 日志观测**：查看结构化运行日志。

### 3.3 功能性需求

综合角色用例，系统的功能性需求可归纳为以下六大模块：

1. **身份与登录**：学校账号制，不使用邮箱或社交登录；
2. **学生学习**：项目、会话、日常会话归档、挑战、认知概览；
3. **教师教学**：教师看板、教师问答、学习记录核实、AI 预审；
4. **管理员治理**：用户、班级、Provider、Capability、MCP、Prompt、日志、导出；
5. **AI 能力**：学生会话、教师问答、篇目识别、布鲁姆识别、挑战生成与评估、预审、Embedding；
6. **数据治理**：SFT 样本、DPO 样本、审阅元数据样本、批次导出。

### 3.4 非功能性需求

#### 3.4.1 安全需求

- **N1**：学生数据对其他学生不可见；
- **N2**：教师只能看到自己负责班级的学习记录；
- **N3**：管理员永远不看学生原始会话，只看教师处理过的可导出样本；
- **N4**：所有 RLS 策略默认拒绝，应用层漏判不能造成数据越权。

#### 3.4.2 可信性需求

- **N5**：原始 AI 回答不能直接进入可导出样本；
- **N6**：只有教师"确认无误"或"修订回答"的回答才能进入 SFT；
- **N7**：只有教师"修订回答"的回答才能进入 DPO，chosen 为修订版、rejected 为原答案；
- **N8**：日常会话归档不进入 SFT/DPO、不生成挑战依据。

#### 3.4.3 可用性需求

- **N9**：学生从"学习提问"首次提问到看到 AI 回答的端到端延迟应低于 6 秒（非流式首 token）；
- **N10**：教师审阅页切换班级 / 学生 / 项目 / 会话应接近瞬时（P95 < 500ms）；
- **N11**：学生侧与教师侧之间的教师修订可见性延迟应控制在 3 秒以内。

#### 3.4.4 可扩展与可治理需求

- **N12**：支持在不改代码的情况下切换 AI Provider 与模型；
- **N13**：新增布鲁姆层级外的认知框架应只需扩展枚举与 UI，不需修改核心业务表结构；
- **N14**：schema 变化必须通过 `web/supabase/migrations/*.sql` 提交，并在云端真源上完成核验。

### 3.5 约束与边界

本课题明确了以下约束：

- **不做邮箱登录、公共注册、社交登录**；
- **不重建本地 FastAPI / SQLite / Chroma 运行时**；
- **不允许本地 Supabase 业务库替代云端**；
- **学生账号在第一阶段只能归属一个班级**（由 `class_memberships_one_student_class_idx` 强制）；
- **项目不可删除，只能清空**（由 `prevent_text_project_delete` 触发器强制）。

这些约束在第 5 章数据库设计中会以硬约束的形式固化。

---

## 第四章 系统总体设计

本章描述文韵智途的总体架构、业务闭环、模块划分与关键设计决策。

### 4.1 总体架构

系统采用经典的分层架构，由表现层、业务交互层、数据服务层、AI 能力层、运维治理层五层组成。整体架构如图 4-1 所示（参见 `web/ppt-assets/charts/02-system-architecture.svg`）。

```
┌──────────────────────────────────────────────────────────┐
│                    表现层 (Presentation)                  │
│  ┌──────────────┬───────────────┬─────────────────────┐   │
│  │ 学生端 UI    │ 教师端 UI     │ 管理员端 UI          │   │
│  │ (Base UI+TW) │ (Base UI+TW)  │ (Base UI+TW)         │   │
│  └──────────────┴───────────────┴─────────────────────┘   │
└──────────────────────────────────────────────────────────┘
                          ▲
                          │ React Server Components / Server Actions
                          ▼
┌──────────────────────────────────────────────────────────┐
│                业务交互层 (Next.js Route Handlers)        │
│  /api/student/chat, /api/teacher/audit, /api/challenge/* │
│  /api/admin/datasets, /api/admin/providers, /api/logs     │
└──────────────────────────────────────────────────────────┘
                          ▲
                          │
                          ▼
┌──────────────────────────────────────────────────────────┐
│              AI 能力层 (Vercel AI SDK + MCP)              │
│  Provider 解析  │  Capability 路由  │  MCP 工具编排         │
└──────────────────────────────────────────────────────────┘
                          ▲
                          │
                          ▼
┌──────────────────────────────────────────────────────────┐
│            数据服务层 (Supabase + PostgreSQL 17)          │
│  Auth │ RLS + 契约触发器 │ pgvector/HNSW │ RPC │ Realtime   │
└──────────────────────────────────────────────────────────┘
                          ▲
                          │
                          ▼
┌──────────────────────────────────────────────────────────┐
│             运维治理层 (日志 / 导出 / 观测)                │
│  data_quality_events │ export_batches │ 结构化日志            │
└──────────────────────────────────────────────────────────┘
```

**表现层**基于 Next.js App Router，三类角色分别有独立的 `src/app/student/`、`src/app/teacher/`、`src/app/admin/` 目录；共享组件位于 `src/components/workbench/`。

**业务交互层**通过 Route Handlers 与 Server Actions 双通道暴露业务能力。前者用于需要流式返回的 AI 会话接口，后者用于表单型的状态变更。

**AI 能力层**封装了 `src/lib/data/common.ts` 的 `getCapability()`，把 capability 名转成运行时的 Provider + model_id + base_url + secret_ref 四元组；`src/lib/mcp-runtime.ts` 再根据角色过滤 MCP 工具。

**数据服务层**以 Supabase 云端项目为唯一真源，通过 `@supabase/ssr` 完成服务端与客户端的凭据隔离。

**运维治理层**聚合 `export_batches` 批次记录、`data_quality_events` 事件表与 `src/lib/observability/server-log-store.ts` 的 JSONL 事件文件。

### 4.2 核心业务闭环

系统的业务主线只有一条——从学生提问到教学数据沉淀的完整闭环，如图 4-2 所示：

```
学生提问 (学习提问空间)
    │
    ▼
篇目识别 + 项目归属 (只对首问调用)
    │
    ├── 能归属 ──▶ 项目会话 ──┐
    │                         ▼
    └── 不能归属 ─▶ 日常会话归档  │
                              │
                              ▼
                  布鲁姆认知路径判定 (仅针对学生问题)
                              │
                              ▼
                        项目级挑战生成
                              │
                              ▼
                     学生答题 + 挑战评估
                              │
                              ▼
                     (通过 → 下一层 / 未通过 → 巩固)
                              │
                              ▼
            教师学习记录核实 (班级→学生→项目→会话)
                              │
                ┌─────────────┼─────────────┐
                ▼             ▼             ▼
             AI 预审       确认无误       修订回答
                              │             │
                              └──┬──────────┘
                                 ▼
                        教师会话级最终提交
                                 │
                                 ▼
                   SFT/DPO 样本物化 + 审阅元数据
                                 │
                                 ▼
              管理员 JSONL 导出 + 导出批次记录
```

这条闭环体现了系统的三条核心设计主张：

1. **过程与确认分离**：会话体现学习过程，挑战体现学习确认；
2. **AI 与教师分工**：AI 处理即时性，教师负责教学正确性；
3. **审阅决定样本**：数据资产只来自教师处理过的内容。

### 4.3 模块划分

系统实际代码按以下模块组织（根目录 `web/src/`）：

| 模块 | 主要目录 / 文件 | 职责 |
| --- | --- | --- |
| 学生端 | `app/student/` | 学习提问空间、项目重定向、挑战确认、学生看板与认知矩阵 |
| 教师端 | `app/teacher/` | 教师看板、教师问答、学习记录核实、教师分析 |
| 管理员端 | `app/admin/` | 用户、班级、Provider、MCP、Prompt、日志、导出 |
| API Handlers | `app/api/` | RESTful + 流式 AI 接口 |
| 身份与会话 | `lib/auth.ts`, `lib/session.ts`, `lib/school-login.ts` | 学校账号校验、会话签名 |
| AI 能力层 | `lib/data/common.ts`, `lib/mcp-runtime*.ts` | capability 解析、MCP 工具运行时 |
| 学生会话合约 | `lib/student-chat-contract.ts` | 首问归属、容器切换、路由跳转 |
| 挑战流程 | `app/api/challenge/{generate,evaluate}/route.ts`, `components/workbench/challenge-client.tsx`, `lib/data/student.ts`, `lib/challenge-progression.ts` | 挑战生成、作答、评估与攀升进度管理 |
| 教师预审 | `lib/teacher-pre-review.ts` | AI 预审结果归一化、Issue 匹配 |
| 样本物化与导出 | `lib/dataset-export.ts`, `lib/data/conversation-finalization.ts` | 会话级最终提交、SFT / DPO / metadata 构造 |
| 加密层 | `lib/crypto/secret-cipher.ts` | Provider / MCP 密钥加密解密 |
| 数据访问层 | `lib/data/{student,teacher,admin,retrieval}.ts` | 按角色隔离的数据访问 |
| UI 组件 | `components/workbench/*.tsx` | 业务组件（Bloom 阶梯、项目卡、挑战客户端等） |

### 4.4 关键设计决策

**D1：云端 Supabase 作为唯一真源**。本项目在任何场景下（开发、联调、验收）都以云端 Supabase 为数据真源。本地仅保留 `web/supabase/migrations/` 作为 schema-as-code，本地实例只能作为一次性的 CLI 演练环境，不承载任何业务数据。

**D2：产品规则下沉到 DB 层**。系统的所有关键产品规则（学生单班、项目不可删除、会话归属、审阅契约等）都通过 RLS 策略与 `*_validate_contract` 触发器固化在数据库。应用层即便漏判，也不会造成数据越权或合约违反。

**D3：AI 能力路由分层绑定**。为应对"学生侧用 flash 模型、教师预审用 advanced 模型"一类的实际需求，系统引入 `scenario_tier_bindings` 与 `model_tier_bindings` 两级绑定，再通过 `rebuild_scenario_provider_capabilities()` RPC 同步到 `provider_capabilities`。管理员在后台一键切换即可生效，应用层不需要任何代码修改。

**D4：教师修订草稿与最终提交分离**。教师修订一条回答后，修订会立即对学生生效，但不进入 SFT / DPO。只有教师对整条会话执行**会话级最终提交**之后，系统才基于该会话最终状态物化样本。这样既保证学生体验即时性，又保证样本的整体一致性。

**D5：日常会话归档作为兜底**。当篇目识别失败或结果不可靠时，系统把对话落入日常会话归档，学生仍可继续使用，但此类会话不参与项目统计、布鲁姆认知路径、挑战生成与 SFT/DPO 导出。

**D6：自定义学校会话 + RLS 函数桥**。本项目使用学校账号登录，没有 `auth.uid()` 可用。通过 `current_app_user_id()`、`has_valid_app_session_signature()` 等函数桥，把应用层的会话身份映射到数据库 RLS 的判断函数中。

### 4.5 小结

本章给出了文韵智途的整体架构、业务闭环与模块划分，并说明了六项关键设计决策。下一章将进入具体的数据库设计。

---

## 第五章 数据库设计

> 本章以云端 Supabase 实例、`web/supabase/migrations/`、`web/src/lib/supabase/database.types.ts` 与应用代码调用点交叉核验。表与字段以云端 schema 读取和类型文件为主，RLS、触发器、RPC 等数据库行为以 migration 记录和代码调用为证据；精确线上数量以 Supabase catalog 与远程 migration 历史为最终口径。

### 5.1 设计原则

本项目的数据库设计遵循五条原则：

1. **云端唯一真源**：任何 schema 变更都必须最终体现在云端 Supabase 实例，本地 migration 只是 schema-as-code 的记录；
2. **规则下沉**：涉及可见性、归属、契约完整性的规则不能只写在应用层，必须在 RLS 或触发器层兜底；
3. **软删除优先**：会话、教师问答等记录默认使用 `deleted_at` 软删除，并配合条件偏索引减少索引膨胀；
4. **枚举化强类型**：所有有限取值字段（角色、能力、审阅类型、审阅状态、导出状态等）使用 PostgreSQL ENUM 而非自由文本；
5. **能力与 Provider 解耦**：通过 Provider / Capability / Tier / Scenario 分层绑定把"能力—模型—Provider"三者完全解耦。

### 5.2 整体 E-R 视图

系统共有 18 张业务表。按聚合分组如下：

| 聚合 | 表 |
| --- | --- |
| **身份与班级** | `profiles`, `classes`, `class_memberships` |
| **学习过程** | `text_projects`, `conversations`, `conversation_messages`, `practice_records` |
| **RAG 语料** | `documents`, `document_chunks` |
| **教学数据** | `audit_records`, `export_batches` |
| **AI 能力配置** | `provider_configs`, `provider_capabilities`, `mcp_servers`, `prompt_presets`, `model_tier_bindings`, `scenario_tier_bindings` |
| **运维观测** | `data_quality_events` |

关键的外键关系可以概括为：`profile → class_membership → class`；`profile → text_project → conversation → conversation_message`；`conversation_message → audit_record`；`text_project/conversation → document → document_chunk`；`provider_config → provider_capability`；`provider_config → model_tier_binding`。

### 5.3 枚举类型

云端共定义 7 种 PostgreSQL ENUM：

| 枚举 | 取值 | 用途 |
| --- | --- | --- |
| `app_role` | `admin`, `teacher`, `student` | 用户角色 |
| `audit_kind` | `sft`, `dpo`, `metadata` | 审阅样本类型 |
| `audit_status` | `pending`, `approved`, `rejected`, `exported` | 审阅状态 |
| `export_status` | `queued`, `ready`, `failed` | 导出批次状态 |
| `interaction_source` | `student_chat`, `teacher_chat`, `practice` | 会话类型 |
| `prompt_preset_status` | `draft`, `published`, `disabled` | Prompt 预设状态 |
| `provider_capability` | `student_chat`, `teacher_chat`, `bloom_classification`, `project_classification`, `practice_generation`, `practice_evaluation`, `audit_assist`, `embedding` | AI 能力枚举 |

> **注意与代码类型层的差异**：应用 TypeScript 类型中还抽象了 `model_tier`（如 `flash / advanced`）以服务模型等级绑定；云端表结构读取中相关 `tier` 字段以文本约束形式呈现，因此本节只把已核验的数据库业务 ENUM 统计为 7 种。`audit_kind` 已扩展为 `('sft','dpo','metadata')`，其中 `metadata` 用于存储由"会话级最终提交"生成的审阅元数据样本。

### 5.4 核心表结构

本节给出与业务最相关的 10 张表的字段与约束。为控制篇幅，部分冗余字段合并表达。

#### 5.4.1 `profiles`（用户账号）

| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `uuid` PK → `auth.users.id` | 主键 |
| `login_id` | `text` UNIQUE | 8 位学号 / 工号 |
| `password_hash` | `text` | 口令哈希（由 `verify_password()` 校验） |
| `role` | `text` check in `(admin,teacher,student)` | 角色 |
| `display_name` | `text` NOT NULL | 姓名 |
| `status` | `text` default `active` | `active | disabled` |
| `created_at`, `updated_at` | `timestamptz` | 审计字段 |

索引：`idx_profiles_login (login_id)`、`idx_profiles_role (role)`、主键 + 唯一。

#### 5.4.2 `classes` / `class_memberships`（班级与成员）

班级本身字段简单（`name`, `grade`, `status`, `created_by`）。重点在成员表 `class_memberships` 的**两条唯一约束**：

- `class_memberships_class_id_profile_id_key UNIQUE (class_id, profile_id)`：同一人同一班不重复；
- `class_memberships_one_student_class_idx UNIQUE (profile_id) WHERE role = 'student'`：**学生单班硬约束**，通过部分唯一索引实现，教师多班不受此限。

这一条"部分唯一索引"把产品规则（学生不可同时属于多个班级）落在了数据库层，比应用层 `count(*)` 校验更安全。

#### 5.4.3 `text_projects`（篇目项目）

```sql
create table public.text_projects (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references profiles(id) on delete cascade,
  class_id uuid references classes(id),
  title text not null,
  author text,
  text_type text not null default 'poem',
  classification_state text not null default 'pending'
    check (classification_state in ('pending','classified','failed','manual')),
  highest_bloom_level integer check (highest_bloom_level between 1 and 6),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
```

关键约束：

- `text_projects_owner_title_normalized_key UNIQUE (owner_id, lower(trim(title)))`：**同一学生、同一篇目标题**只能有一个项目。`lower(trim(...))` 表达式索引保证大小写、前后空格不会绕过唯一性；
- `text_projects_prevent_delete` 触发器：**项目不可删除**，即便所有会话被清空，项目仍保留为空项目状态；
- `text_projects_sync_contract` 触发器：写入时校验 `classification_state` 与 `highest_bloom_level` 之间的契约；
- `practice_records_sync_project_bloom` 触发器在挑战记录变化时调用 `refresh_project_highest_bloom_level(project_id)`，自动同步 `highest_bloom_level`。

#### 5.4.4 `conversations`（会话）

| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `uuid` PK | |
| `owner_id` | `uuid` NOT NULL → profiles | 会话所属学生 / 教师 |
| `project_id` | `uuid` NULL → text_projects | 项目归属；为空即"日常会话归档" |
| `class_id` | `uuid` NULL → classes | 班级冗余字段用于教师 RLS |
| `source` | `interaction_source` | `student_chat / teacher_chat / practice` |
| `prompt_preset_id` | `uuid` NULL → prompt_presets | 使用的 Prompt 预设 |
| `title` | `text` default `'新对话'` | 会话标题 |
| `deleted_at` | `timestamptz` NULL | 软删除标记 |

索引：

- `conversations_active_owner_idx (owner_id, updated_at DESC) WHERE deleted_at IS NULL`：未删除列表高效拉取；
- `conversations_daily_archive_idx (owner_id, updated_at DESC) WHERE source='student_chat' AND project_id IS NULL AND deleted_at IS NULL`：**日常会话归档**专用偏索引；
- `idx_conversations_project (project_id)`、`idx_conversations_owner (owner_id)`。

触发器：`conversations_validate_contract` 校验 `source + class_id + project_id` 的组合契约，例如不允许 `teacher_chat` 会话挂到 `text_projects` 上。

#### 5.4.5 `conversation_messages`（对话消息）

| 列 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `uuid` PK | |
| `conversation_id` | `uuid` NOT NULL | |
| `role` | `text` check `(user,assistant,system,tool)` | |
| `content` | `text` | 展示型内容（教师修订后即为修订内容） |
| `parts` | `jsonb` | AI SDK 多段消息结构，含原始回答备份 |
| `bloom_level` | `integer` check 1..6 | 该问题的布鲁姆认知路径最高层 |
| `bloom_state` | `text` check `(pending,classified,failed,unclassified)` | |
| `model_id` | `text` | |

`conversation_messages_validate_contract` 触发器把"parts 中必须保留原 AI 回答"这类契约固化在 DB 层，同时实现三条关键阻止规则：
- 阻止向已软删除（`deleted_at IS NOT NULL`）的会话插入消息；
- 阻止学生（`role='user' + source='student_chat'`）向已 finalized 的会话插入消息——通过查询 `audit_records` 中是否存在 `kind='metadata' + status in ('approved','exported') + metadata->>'teacher_action'='conversation_finalized'` 来判定；
- 布鲁姆认知路径只对 `role='user' + source='student_chat' + project_id IS NOT NULL` 的消息生效，其他消息自动设为 `bloom_state='unclassified'`。

#### 5.4.6 `practice_records`（挑战记录）

挑战记录是单独的聚合，不与会话直接耦合，体现"挑战不依赖会话，只依赖项目与目标层级"的设计：

| 列 | 类型 |
| --- | --- |
| `id` | `uuid` PK |
| `student_id` | `uuid` NOT NULL → profiles |
| `project_id` | `uuid` → text_projects |
| `target_bloom_level` | `smallint` check 1..6 |
| `prompt`, `answer`, `feedback` | `text` |
| `achieved` | `boolean` |
| `evaluation_state` | `text` default `pending` |

挑战记录通过 `practice_records_sync_project_bloom` AFTER INSERT/UPDATE/DELETE 触发器自动把最新的"通过层级"反写到对应项目的 `highest_bloom_level`。

#### 5.4.7 `documents` / `document_chunks`（RAG 语料与会话作用域向量）

```sql
create table public.document_chunks (
  id uuid primary key default gen_random_uuid(),
  document_id uuid not null references documents(id) on delete cascade,
  owner_id uuid not null references auth.users(id) on delete cascade,
  class_id uuid references classes(id) on delete set null,
  project_id uuid references text_projects(id) on delete set null,
  conversation_id uuid references conversations(id) on delete set null,
  chunk_index integer not null,
  content text not null,
  token_count integer,
  metadata jsonb not null default '{}'::jsonb,
  embedding extensions.vector(768) not null,
  created_at timestamptz not null default now(),
  unique (document_id, chunk_index)
);

create index document_chunks_embedding_hnsw
  on public.document_chunks using hnsw (embedding vector_cosine_ops);

create index document_chunks_conversation_id_idx
  on public.document_chunks (conversation_id) where conversation_id is not null;
```

- `embedding vector(768)` 与当前代码中的 `embedText(chunk, 768)`、会话检索 768 维常量一致；更换为不同维度的嵌入模型需要新增列或重建索引；
- HNSW + cosine 是 pgvector 在当前语料量下最合适的组合；
- `owner_id / class_id / project_id / conversation_id` 四级作用域冗余字段用于：
  - RLS 策略逐级放行；
  - RPC `match_conversation_document_chunks()` 按会话级筛选向量。

#### 5.4.8 `audit_records`（审阅样本 / 元数据）

审阅记录表是系统中最复杂的聚合之一，承担了 SFT 样本、DPO 偏好对、审阅元数据三种形态：

| 列 | 说明 |
| --- | --- |
| `kind` | `sft / dpo / metadata` |
| `status` | `pending / approved / rejected / exported` |
| `source_message_id`, `source_conversation_id`, `class_id` | 定位来源 |
| `auditor_id` | 审阅教师 |
| `prompt` | 样本的 user prompt |
| `original_answer`, `corrected_answer` | SFT 用的原 AI 回答 / 教师修订回答 |
| `chosen_answer`, `rejected_answer` | DPO 用的偏好对 |
| `metadata` | `jsonb`，存放 `teacher_action`、学生匿名 ID、样本关联 ID |
| `quality`, `rationale` | 教师标注 |
| `exported_at`, `updated_at` | 审计字段 |

关键设计：

- `audit_records_exportable_idx (kind, status, updated_at DESC) WHERE status in ('approved','exported')`：**按导出维度的条件偏索引**，显著加速按时间倒序的可导出样本查询；
- `audit_records_validate_contract` 触发器根据 `kind` 校验必填字段：例如 `kind='dpo'` 要求 `chosen_answer` 与 `rejected_answer` 均非空；
- `audit_app_student_finalization_read` RLS 策略只让学生看到自己的、由"会话级最终提交"产生的 `kind='metadata'` 审阅元数据，不会暴露其他教师审阅数据。

#### 5.4.9 `export_batches`（导出批次）

```sql
create table public.export_batches (
  id uuid primary key default gen_random_uuid(),
  export_type audit_kind,       -- sft / dpo / metadata
  status export_status default 'ready',
  record_count integer default 0,
  jsonl text,
  created_by uuid references profiles(id),
  created_at timestamptz default now()
);
```

导出批次直接保存 JSONL 文本；在第 11 章会讨论大样本情况下的优化建议。

### 5.5 RLS 策略与身份函数

#### 5.5.1 身份函数

| 函数 | 用途 |
| --- | --- |
| `current_app_user_id()` | 返回当前应用层登录用户 ID，是自定义学校会话的核心桥梁 |
| `current_profile_role()` | 返回 `app_role` |
| `is_admin()` | 判断是否管理员 |
| `teacher_can_access_class(class_id)` | 通过 `class_memberships` 判断教师是否可访问班级 |
| `has_valid_app_session_signature()` | 校验自定义会话签名 |
| `is_student_conversation_finalized(conversation_id)` | 判断会话是否进入最终提交状态 |

#### 5.5.2 关键 RLS 策略

- `profiles_app_select`：`id = current_app_user_id() OR is_admin()`；
- `classes_app_member_select`：仅班级成员与管理员可见；
- `conversations_owner_all`：`owner_id = current_app_user_id() OR is_admin()`；
- `conversations_teacher_read`：`class_id IS NOT NULL AND teacher_can_access_class(class_id)`；
- `messages_conversation_scope`：按会话 owner / 教师班级 / 管理员三路放行；
- `messages_teacher_update`：**仅教师可以更新学生会话的 AI 回答**，且必须是自己班级、会话未软删除、来源为 `student_chat`；
- `audit_app_student_finalization_read`：学生只能看到自己的 `metadata` 审阅记录；
- `audit_app_teacher_admin_read`：教师能看自己班级的审阅记录，管理员能看全部。

这一整套策略保证了第 3.4 节 N1–N4 安全需求能够在数据库层被硬约束。

### 5.6 关键 RPC 与触发器

#### 5.6.1 RPC 与身份 / 能力 helper 函数

| 函数 | 作用 |
| --- | --- |
| `authenticate_school_account(p_login_id, p_password)` | 学校账号登录，返回 id/login_id/role/display_name |
| `get_model_tier_provider(p_tier)` | 解析 `flash/advanced` 等级对应的 Provider 与 model_id |
| `get_provider_capability_provider(p_capability)` | 解析 capability 对应的 Provider 配置 |
| `match_document_chunks(...)` | 向量检索，按 project / class / owner 作用域 |
| `match_conversation_document_chunks(...)` | 会话级 RAG 检索 |
| `save_model_tier_binding_and_sync(p_tier, p_provider_id, p_model_id)` | 保存等级绑定并同步 `provider_capabilities` |
| `save_scenario_tier_bindings_and_sync(p_bindings jsonb)` | 批量保存 capability 等级绑定 |
| `refresh_project_highest_bloom_level(p_project_id)` | 重算项目最高层级 |
| `rebuild_scenario_provider_capabilities()` | 管理员后台一键重建能力路由 |

#### 5.6.2 触发器概览

本次核验中，与业务规则相关的触发器主要分为三大类：

- **`*_touch`**：`touch_updated_at` 更新 `updated_at`；
- **`*_validate_contract`**：契约式校验，例如 `conversations_validate_contract` 约束 `(source, class_id, project_id)` 组合；
- **业务衍生**：`practice_records_sync_project_bloom`、`text_projects_prevent_delete`、`text_projects_sync_contract` 等。

### 5.7 数据库设计总结

整套数据库设计把"学生学习—教师核实—管理员治理"的业务主线落在 18 张业务表、7 种核心业务枚举，以及围绕 RLS、契约触发器、RPC / helper 函数组织的数据库规则上。核心特征可以总结为：

1. **强类型化**：所有分类字段使用 PostgreSQL ENUM；
2. **强约束**：部分唯一索引（学生单班）+ 触发器（项目不可删、契约校验）；
3. **强审计**：`deleted_at` 软删除、`updated_at` 自动维护、`data_quality_events` 事件表；
4. **强可见性控制**：RLS 对每类角色都有独立策略；
5. **强扩展性**：能力路由、MCP 工具、Prompt 预设都以数据驱动。

第 11 章会针对当前 schema 提出进一步的优化建议。

---

## 第六章 学生端设计与实现

### 6.1 功能概览

学生端承担系统的"学习入口 + 学习沉淀 + 学习确认"三类职责，页面路由如下：

| 路由 | 作用 |
| --- | --- |
| `/student` | 学习提问空间：项目与日常会话归档并列展示，承载会话继续提问 |
| `/student/projects` | 兼容入口，重定向到 `/student/me` |
| `/student/projects/[projectId]` | 兼容入口，重定向到 `/student?projectId=...` |
| `/student/challenge` | 挑战入口：筛选项目 + 查看攀登路线 + 作答 |
| `/student/challenge/[projectId]` | 兼容入口，重定向到 `/student/challenge?projectId=...` |
| `/student/me` | 学生看板：项目卡片墙、个人认知矩阵与挑战确认分布 |

### 6.2 学习提问与项目归属

学生从全局空白的"学习提问"入口发起首问时，系统流程如下：

```
[学生提交首问]
   │
   ├─▶ 前端立即显示学生消息 + AI 思考状态
   ├─▶ 并发调用：
   │     ① 篇目识别 (project_classification capability)
   │     ② AI 首答 (student_chat capability，可带 MCP 工具)
   │
   ├─▶ 篇目识别结果：
   │     - 可靠命中 → 自动 upsert 到 text_projects → 将会话归属该项目
   │     - 不可靠 / 失败 → 会话保持 project_id = NULL（日常会话归档）
   │
   └─▶ AI 回答结束后，对本条学生问题调用 bloom_classification capability
         → 更新 conversation_messages.bloom_level / bloom_state
```

这里的关键不变式是："归属只决定会话容器，不限制 AI 在回答中引用其他篇目"。实现上，`src/lib/student-chat-contract.ts` 提供了 `shouldClassifyProjectForStudentTurn()`、`buildStudentChatRequestBody()`、`buildStudentConversationHref()` 等纯函数，集中管理这一逻辑，确保服务端 Route Handler 与客户端逻辑严格一致。

### 6.3 学生看板

学生看板位于 `/student/me`，使用卡片墙而非列表，每张卡片展示：

- 项目标题（篇目）；
- 当前已确认的最高布鲁姆层级 `highest_bloom_level`；
- 挑战状态（等待挑战确认 / 待巩固 / 需要巩固 / 已完成六层认知挑战确认）；
- 最近学习时间（`max(updated_at)`）。

卡片点击行为是"进入该项目的空白会话"，而不是"打开最近一条会话"，以避免学生被旧会话绑住。日常会话归档不在看板展示，只能从学习提问空间进入。

### 6.4 挑战引擎

挑战流程是学生侧最有特色的模块。当前学生挑战页的直接调用链由 `src/components/workbench/challenge-client.tsx`、`src/app/api/challenge/generate/route.ts`、`src/app/api/challenge/evaluate/route.ts`、`src/lib/data/student.ts` 与 `src/lib/challenge-progression.ts` 共同完成；`src/lib/challenge-engine.ts` 中保留的旧版结构化封装不是当前页面的直接调用入口。

#### 6.4.1 单题面 + 作答提示

当前生成接口要求模型返回结构化对象：

```ts
{
  prompt: string,
  guidance: string
}
```

其中 `prompt` 是直接展示给学生的一道挑战题，`guidance` 是一句简短作答提醒，并保存到 `practice_records.feedback`。提示词要求"只出 1 道题，任务单一清楚，避免一题多问"，并按目标层级选择题型：低层级偏向找信息、说意思、找依据，高层级可采用比较分析、观点判断、迁移应用、仿写改写等形式。这样既保留了"结合原文依据完成挑战"的教学意图，又与当前代码中的单题面实现保持一致。

#### 6.4.2 输入最小化原则

挑战生成提示词只输入三类业务信息：

- 篇目标题（通过 `text_projects.title`）；
- 项目下最近的学生问题（通过 `conversation_messages` JOIN `conversations` 按 `project_id + deleted_at IS NULL + role='user'` 筛选，取最近 8 条）；
- 当前目标层级（请求体传入或由 `text_projects.highest_bloom_level` 推导下一层）。

**不输入**任何 AI 回答、教师修订内容、SFT/DPO 数据与教师问答历史。系统提示词中明确禁止引用"布鲁姆""认知水平确认""达标""评估"等后台话术，也禁止引用 AI 回答、教师修订、学习记录核实或 SFT/DPO 流程。这条原则既保护了学生隐私，也避免挑战生成"向 AI 输出风格偏置"。

#### 6.4.3 层级推进与接口边界

学生界面不提供任意层级选择，而是根据当前已确认层级显示下一挑战层级。后端生成接口会校验学生身份、项目归属和 `targetBloomLevel` 的 1 到 6 范围；如果请求体未传入目标层级，则默认使用 `highest_bloom_level + 1`。挑战评估接口只根据本条 `practice_records` 中保存的目标层级、题目与学生作答判断是否通过，明确不参考 AI 回答、教师修订或项目最高层级来跨级确认。

更严格的"后端拒绝跳级请求"可作为后续业务契约优化；当前主流程通过前端入口、数据读取和评估规则保证学生自然沿 L1 到 L6 攀升。

#### 6.4.4 进度与状态机

挑战进度在学生看板和挑战页中由 `practice_records` 计算，核心状态为：

- **已通过**：`practice_records.achieved = true`；
- **待巩固**：同一目标层级失败 1 次；
- **需要巩固**：同一目标层级连续失败 ≥ 2 次；
- **已完成六层**：所有六层均通过。

失败后不立即降级，学生仍停留在该目标层级，主行动提示优先引导回项目提问。

### 6.5 布鲁姆认知矩阵与路径边界

当前实现不再保留独立的 `/(student)/cognitive-path/[projectId]` 路由。学生侧把项目概览和认知矩阵收敛到 `/student/me`：项目卡片展示问题数量、挑战记录数量、最高确认层级与状态；认知矩阵按项目展示 L1 到 L6 的挑战确认状态。这里特别区分了"会话级统计"与"项目级分布"：

- **会话级统计**：每个会话内所有学生问题各自路径的最高充分层次分布；
- **项目级分布**：只来自 `practice_records` 通过结果，会话内容不直接进入该分布。

### 6.6 小结

学生端的设计原则是"一问即入、不强迫项目化、严格区分过程与确认"。实现层面通过 Server Actions + Route Handlers + Server Components 完成身份判断与权限过滤，所有数据越权风险由 RLS 兜底。

---

## 第七章 教师端设计与实现

### 7.1 教师端三工作区

教师端在侧边栏明确承载三个并列的一级工作区：**教师看板（`/teacher`）、教师问答（`/teacher/chat`）、学习记录核实（`/teacher/audit`）**。另有教师分析（`/teacher/analytics`）作为看板延伸，不单设一级入口。

系统明确区分这三个工作区的职责：

| 工作区 | 面向 | 不做什么 |
| --- | --- | --- |
| 教师看板 | 宏观大盘 + 优先级入口 | 不完成核实动作，不放教师问答入口 |
| 教师问答 | 教师个人 AI 问答 + 提示词模板 | 不参与学生会话核实，不进 SFT/DPO |
| 学习记录核实 | 班级→学生→项目→会话的四级核实 | 不是扁平待办队列，不做抽象风险摘要 |

### 7.2 学习记录核实的四级导航

核实页面的左栏是四级导航，右栏是完整会话：

```
班级总览 ──▶ 学生层 ──▶ 项目层 ──▶ 会话层
   │           │          │          │
   │           │          │          ▼
   │           │          │       完整会话 + 每条 AI 回答气泡内的修订入口
   │           │          │       + AI 预审标红片段
   │           │          ▼
   │           │       篇目名 + 待核实会话数 + 风险回答数 + 会话数 + 挑战概览
   │           ▼
   │        姓名 + 待核实会话数 + 风险回答数 + 项目数 + 最近学习时间
   ▼
班级卡片：待核实会话数 + 风险回答数 + 最近学习时间
```

设计上拒绝"单条 AI 回答入口"，避免退化为待办队列。左栏会话项只显示时间、待提交 AI 回答数、风险回答数和布鲁姆认知路径摘要；每条回答的处理在右栏的完整会话语境中完成。

### 7.3 AI 预审

AI 预审目的是把教师的注意力指向疑似问题段，由 `audit_assist` capability 承担。实现采用**两阶段策略**：

**第一阶段：全会话预审**。`runPreReview()` 使用 `generateObject()` + `conversationPreReviewSchema`（Zod schema）对整个会话的所有 AI 回答做一次性预审。系统提示词要求模型对每条 AI 回答都返回一项结果（即使无疑点也返回 `issues: []`），并严格使用后端传入的 `messageId`。

**第二阶段：单条补审**。第一阶段结束后，`normalizePreReviewResults()` 检查每条 AI 回答是否有有效结果（`status='checked'`）。对于 `missing_result`（模型遗漏）或 `ignoredIssueCount > 0`（quote 无法匹配原文被丢弃）的回答，系统逐条发起 `singleMessagePreReviewSchema` 的补审调用，确保覆盖率。

归一化逻辑（`src/lib/teacher-pre-review.ts`）负责：

1. 用 `containsPreReviewQuote()` 函数模糊匹配 Issue 的 `quote` 到回答内容（折叠空白 + Markdown 标记后做子串匹配），避免 AI 幻觉出不存在的句段；
2. 按每条回答最多 4 个 Issue 做节流，**不做会话级全局截断**（CONTEXT.md 硬规则）；
3. 覆盖判定严格要求每条 AI 回答都有预审结果，没有匹配的 quote 就标为 `missing_result`；
4. 最终写入 `audit_records`（`kind='metadata', quality='pre_review'`），`metadata` 中包含 `reviewed_message_ids`、`missing_message_ids`、`message_results`（逐条结果）和 `issues`（所有疑点）。

#### 7.3.1 预审的幂等性与持久状态

教师进入审阅页不会自动重新调用模型，只有在点击重新发起 AI 预审按钮后才会触发新的预审调用。预审结果保存为 `audit_records` 行，前端通过查询该行的 `metadata` 渲染标红状态和"AI 已预审 / 无明显疑点"提示，不依赖 Server Action 的临时反馈。如果 `review_status='partial'`（有 `missing_message_ids`），界面显示"待补审"状态并提示教师再次点击。

### 7.4 确认无误与修订回答

教师修订回答的流程严格区分"草稿修订"与"会话级最终提交"：

**草稿修订**（`reviseLearningRecord()`）：

1. 教师在 AI 回答气泡中直接编辑，提交修订版 `corrected_answer`；
2. 系统立即更新 `conversation_messages.content` 为修订版，`parts` 写入 `[{type:'text',...}, {type:'data-teacher-revision', data:{revised:true, reviewedAt}}]`；
3. 同时写入一条 `audit_records`（`kind='metadata', status='approved', quality='revision_draft'`），`metadata.teacher_action='revision_draft'`，保留 `original_answer` 和 `corrected_answer`；
4. 通过 `broadcastStudentConversationUpdate()` 通知学生侧刷新；
5. **不生成 SFT/DPO**，会话不进入已核实状态，学生仍可继续追问。

如果 `conversation_messages` 的 UPDATE 被 RLS 拒绝（`messages_teacher_update` policy 要求 `source='student_chat' + class_id IS NOT NULL + teacher_can_access_class(class_id) + deleted_at IS NULL`），系统返回明确错误提示。

**会话级最终提交**（`finalizeLearningConversation()`）：

1. 遍历会话中所有 `role='assistant'` 的消息；
2. 对每条消息，检查是否有 `revision_draft`（通过 `latestRevisionDraft()` 查找最新的 `kind='metadata' + teacher_action='revision_draft'` 审阅记录）；
3. 检查是否已有 `existingFinalizedKinds`（避免重复物化）；
4. **有修订的回答**：物化为 `kind='sft'`（`corrected_answer` 作为 assistant 内容）+ `kind='dpo'`（`chosen_answer=修订版, rejected_answer=原回答`），`metadata.conversation_action='conversation_finalized'`；
5. **未修订的回答**：物化为 `kind='sft'`（`original_answer` 即当前 AI 回答作为 assistant 内容），不生成 DPO；
6. 最后写入一条 `kind='metadata', teacher_action='conversation_finalized'` 的审阅记录，包含 `assistant_count / confirmed_count / revised_count / materialized_record_count`；
7. 通过 `broadcastStudentConversationUpdate()` 通知学生侧会话已锁定；
8. 数据库层 `validate_conversation_message_contract` 触发器阻止学生向 finalized 会话插入新消息。

SFT 样本的 `prompt` 字段使用 `nearestPrompt()` 函数从会话 transcript 中向前查找最近的 `role='user'` 消息内容，保留该回答之前的上下文语义。

### 7.5 教师问答与提示词模板

教师问答页保留教师个人 AI 问答历史，每条独立删除。提示词模板以卡片形式内联在问答界面中，点击卡片只"填入输入框"不自动发送，并在输入框已有内容时给出"替换 / 追加 / 取消"三选一。加号入口用弹窗创建新模板，只需 `title + system_instruction` 两字段，避开变量语法、版本、发布状态等复杂字段。

### 7.6 小结

教师端的核心设计主张是"效率来自 AI 预审 + 分层定位，不来自牺牲教学语境"。这一主张通过三条硬约束落地：不做扁平待办队列、不单条审阅、不在教师问答中混入学习记录核实。

---

## 第八章 管理员端与 AI 能力治理

### 8.1 管理员侧信息架构

管理员侧边栏分为两组：**学校管理** 与 **AI 运维**。

| 分组 | 页面 | 路由 | 作用 |
| --- | --- | --- | --- |
| 学校管理 | 用户管理 | `/admin/users` | 账号 + CSV 导入 + 角色 / 状态筛选 |
| 学校管理 | 班级管理 | `/admin/classes` | 班级 + 成员分配弹窗 |
| AI 运维 | Provider | `/admin/providers` | Provider + Capability + Tier |
| AI 运维 | MCP | `/admin/mcp` | MCP 服务器 + 工具 + 允许角色 |
| AI 运维 | 提示词预设 | `/admin/presets` | 预设草稿 / 发布 / 禁用 |
| AI 运维 | 日志 | `/admin/logs` | 结构化运行日志 |
| AI 运维 | 数据导出 | `/admin/exports` | SFT / DPO / metadata 批次导出 |

管理员端界面风格延续教育 SaaS 的通用视觉体系，关键信息放在数据表格与筛选器，而非卡片。

### 8.2 用户与班级治理

#### 8.2.1 用户管理

用户管理页展示：

- 角色、状态；
- 班级归属摘要（教师显示负责班级数量，学生显示所在班级或"未分配"）；
- 姓名 / 账号全文搜索 + 角色 / 状态筛选；
- CSV 导入入口。

MVP 阶段不提供"在线改角色、停用账号"，这一约束主要是为了避免权限边界在运维过程中被无意突破。

#### 8.2.2 班级成员分配

班级页的"成员分配"弹窗支持：

- 查看当前成员；
- 按角色添加账号；
- 移除已有成员；
- **学生自动迁班**：把学生加入新班时，自动移除其原有班级关系（落在应用层 + 部分唯一索引双重保护）。

系统允许管理员移除班级最后一个教师，但界面必须提示"移除后该班级暂无负责教师"——这是一条交互硬约束，防止班级因无负责教师而脱管。

### 8.3 AI 能力治理

#### 8.3.1 分层绑定模型

系统的 AI 能力路由由四张表 + 两类同步 RPC 串联：

```
┌─────────────────┐
│ provider_configs │  (id, name, provider_type, base_url, secret_ref, …)
└────────┬────────┘
         │ 1..N
         ▼
┌─────────────────────────┐
│ provider_capabilities   │  (provider_id, capability, model_id, is_enabled)
└────────┬────────────────┘
         │ N..1
         ▼
┌─────────────────────────┐
│ model_tier_bindings     │  (tier, provider_id, model_id)    -- flash / advanced
└────────┬────────────────┘
         │ N..1
         ▼
┌─────────────────────────┐
│ scenario_tier_bindings  │  (scenario, tier)                 -- capability → tier
└─────────────────────────┘
```

例如，把"挑战生成" (`practice_generation`) 绑定为 `advanced` 等级，而 `advanced` 等级绑定到 OpenAI 的 `gpt-4.1-mini`：

```sql
call save_scenario_tier_bindings_and_sync('[
  {"scenario":"practice_generation","tier":"advanced","is_enabled":true}
]'::jsonb);

call save_model_tier_binding_and_sync('advanced', '<provider_id>', 'gpt-4.1-mini');
```

随后 `rebuild_scenario_provider_capabilities()` RPC 会把等级绑定展开到 `provider_capabilities`，使应用层可以直接通过 `getCapability('practice_generation')` 拿到最终的 Provider + model_id。

这样设计的收益有三点：

1. **成本分级**：flash / advanced 两级决策，学生侧可以走便宜模型，教师审阅可以用更强的模型；
2. **供应商切换零代码**：管理员后台一键切换 Provider；
3. **可观测**：每次绑定调整都会生成审计事件（通过应用层日志 + `data_quality_events`）。

#### 8.3.2 Provider 健康检查

Provider 记录包含 `health_status`、`last_health_check_at`、`last_health_latency_ms` 与 `secret_*` 审计字段。后台提供健康检查动作，向 `GET /models` 发起一次轻量探活，成功记录延迟，失败写入 `failed` 并弹出错误提示。

#### 8.3.3 MCP 工具治理

`mcp_servers` 表包含 `allowed_roles`（`app_role[]`）与 `enabled_tools`（jsonb）字段。运行时通过 `getRoleMcpToolsFromSupabase()`（见 `src/lib/mcp-runtime-core.ts`）按角色过滤：

```ts
const { data } = await supabase
  .from('mcp_servers')
  .select('*')
  .eq('is_enabled', true)
  .contains('allowed_roles', [role]);
```

教师、学生分别拿到各自的工具集合，不会相互泄露。

### 8.4 提示词预设

提示词预设是可发布 / 可禁用的资源，面向教师的预设在 `/admin/presets` 管理。状态机 `draft → published → disabled` 保证只有发布态的模板才会出现在教师问答界面。

### 8.5 日志观测

日志由两条路径共同支撑：

- **结构化 JSONL 文件**：由 `src/lib/observability/server-log-store.ts` 负责，追加型写入，适合按时间顺序查询；
- **数据质量事件表 `data_quality_events`**：只写关键的契约违反、导出批次、schema 漂移等事件。

`/admin/logs` 页面把两者合并展示，按 provider / model / capability / error_code / latency 分面筛选。

### 8.6 小结

管理员端完成了系统从"能用"上升到"能管"的跨越。AI 能力治理通过分层绑定模型把"谁来提供能力、哪个模型、属于哪个等级、哪个场景使用哪个等级"解耦为四个独立决策，使平台具备长期演进能力。

---

## 第九章 数据治理与 SFT/DPO 导出

### 9.1 数据治理原则

本系统的数据治理遵循三条原则：

1. **未经教师处理的内容不进入样本**：学生原始会话、日常会话归档、AI 原回答若未被教师处理，均不进入 `audit_records`；
2. **只有会话级最终提交才能物化样本**：单独保存修订只影响学生可见内容，不产出 SFT / DPO；
3. **导出批次不可覆盖**：已导出样本允许再次导出，但每次都会生成新的 `export_batches` 记录。

### 9.2 样本物化流程

教师点击会话级最终提交后，`src/lib/data/conversation-finalization.ts` 按以下规则批量写入 `audit_records`：

| 回答状态 | 产出 SFT | 产出 DPO | 说明 |
| --- | --- | --- | --- |
| 未修订，会话随最终提交 | ✅ 使用 AI 原回答 | ❌ | 视为"确认无误" |
| 草稿修订过 | ✅ 使用教师修订版 | ✅ chosen=修订版，rejected=AI 原回答 | 两条样本绑定同一来源 |
| 被"仅标记无误但未最终提交" | ❌ | ❌ | 仅保存修订不物化样本 |

SFT 样本的 `messages` 保留该回答之前的同一会话多轮上下文，不包含该回答之后的轮次，以维持"训练时刻的真实上下文"。

### 9.3 审阅元数据样本

每次会话级最终提交还会产生一条 `kind='metadata'` 的审阅元数据样本，字段包括：

- `teacher_action: 'conversation_finalized'`；
- `source_conversation_id`、`source_message_id`、`class_id`；
- `auditor_id`；
- `includesSft` / `includesDpo`（用于下游导出聚合）；
- `studentAnonId`（学生匿名标识，用于管理员侧聚合分析而不暴露身份）。

这条元数据样本通过 `audit_app_student_finalization_read` RLS 策略向学生开放只读权限，让学生能看到"这个会话已被教师核实完毕"的状态，而不暴露教师具体的审阅内容。

### 9.4 导出 JSONL

`/admin/exports` 页面允许管理员按条件筛选导出：

- 时间范围；
- 班级；
- 教师；
- 篇目 / 项目；
- 审阅类型（`sft / dpo / metadata`）；
- Scope（`unexported` 默认 / `all`）。

导出时构造三类 JSONL：

```jsonl
// sft.jsonl
{"messages":[{"role":"user","content":"..."},{"role":"assistant","content":"..."}],"metadata":{...}}

// dpo.jsonl
{"prompt":"...","messages":[...],"chosen":"...","rejected":"...","metadata":{...}}

// metadata.jsonl
{"sampleId":"...","sourceConversationId":"...","teacherAction":"conversation_finalized",...}
```

导出结果写入 `export_batches`，`record_count` 与 `status='ready'` 同步更新；已导出的 `audit_records` 被标记 `status='exported'` 并记录 `exported_at`。

### 9.5 `data_quality_events` 事件表

该表承担"异常但不致命"的观测。典型事件包括：

- `event_type='schema_drift'`：本地 migration 与云端真源不一致；
- `event_type='finalization_payload_invalid'`：最终提交时发现契约异常；
- `event_type='export_skipped'`：样本因 RLS / 契约违反被跳过。

第 11 章会对这张表提出改造建议。

### 9.6 小结

数据治理层的核心是"以 `audit_records` 的三种 `kind` 组织教学样本"。整套流程从教师的核实动作自然收敛到管理员的导出动作，不需要单独的后台脚本或定时任务。

---

## 第十章 系统测试与部署

### 10.1 测试策略

本项目在测试上采用"单元测试 + 契约测试 + 端到端流水线测试"三层结构。

#### 10.1.1 单元测试

`web/src/lib/__tests__/` 下共有 9 个测试文件，覆盖以下关键模块：

| 测试文件 | 被测模块 |
| --- | --- |
| `challenge-progression.test.ts` | 挑战进度状态机 |
| `chat-message-parts.test.ts` | AI SDK 多段消息解析 |
| `conversations-deleted-at-guard.test.ts` | 软删除守卫 |
| `dataset-export.test.ts` | SFT/DPO 构造 |
| `mcp-runtime-core.test.ts` | 按角色过滤 MCP 工具 |
| `mcp-runtime-policy.test.ts` | MCP 权限策略 |
| `student-chat-contract.test.ts` | 学生会话归属合约 |
| `teacher-pre-review.test.ts` | 教师预审归一化 |
| `teacher-revision-columns.test.ts` | 教师修订字段合约 |

测试通过 Node 原生 `--test --experimental-strip-types` 运行，不引入额外测试框架，减少了依赖负担。

#### 10.1.2 契约测试

部分规则同时存在于 RLS、触发器与应用层（比如"学生单班"）。项目通过测试用例覆盖应用层逻辑，数据库层由 `*_validate_contract` 触发器在 INSERT/UPDATE 阶段兜底。

#### 10.1.3 端到端流水线测试

`web/scripts/sft-dpo-pipeline-e2e.mjs` 是一个端到端脚本，模拟"学生提问 → AI 回答 → 教师修订 → 会话级最终提交 → 样本导出"全链路，用于在部署前冒烟。

### 10.2 静态校验

根目录 `README.md` 规定了一条"遗留代码静态校验"：

```bash
grep -R "find_login_email\|cwb_token\|jose\|NEXT_PUBLIC_API_BASE_URL\|FastAPI\|SQLite\|Chroma" -n web/src
```

该校验在每次部署前运行，确保项目没有重新引入旧 FastAPI / SQLite / Chroma 栈的痕迹。

### 10.3 部署架构

- **前端 + API**：Next.js 16 运行在 Node 20+，部署方式不限（推荐 Vercel 或自建 Kubernetes）；
- **数据库**：Supabase 云端项目，开启 pgvector 扩展；
- **密钥管理**：Provider / MCP 密钥经过 `src/lib/crypto/secret-cipher.ts` 对称加密后以 `secret_ref` 形式存入 DB，仅保留 `secret_last_four` 便于审计；
- **Realtime**：使用 Supabase Realtime + 自定义会话签名 + `broadcastStudentConversationUpdate()` 主动推送，配合前端 `visibilitychange` 事件与短轮询兜底；
- **Edge Functions**：本项目目前未启用，保留扩展能力。

### 10.4 本地 Migration 与云端对齐

本项目的 `web/supabase/migrations/` 目录经过一次完整清理（2026-05-11），删除了 18 个与云端 migration 历史不一致的早期草稿文件。当前目录只保留 4 个文件：

| 文件 | 作用 |
| --- | --- |
| `00000000000000_baseline_schema.sql` | 参考文件，记录云端 21 条 migration 历史清单 |
| `20260511000001_db_optimization_phase1.sql` | 索引优化 + 冗余 RLS 清理 + search_path 修复 |
| `20260511000002_security_definer_lockdown.sql` | SECURITY DEFINER 函数权限收紧 |
| `20260511060200_block_messages_in_finalized_conversations.sql` | 修复 CONTEXT.md 规则缺口：finalized 会话阻止学生续问 |

云端 `supabase_migrations.schema_migrations` 表包含 22 条记录（含上述 3 条新增），是唯一验收口径。

### 10.5 已知局限

- 本地 Supabase 业务库未被本项目使用；如需本地 schema 演练，必须在 `web/supabase/` 下进行；
- Export JSONL 目前以 `text` 形式存储在 `export_batches.jsonl`，大样本场景会带来单行过长问题，第 11 章给出优化建议；
- Realtime 仅在会话级最终提交与教师修订场景落地，学生 / 教师之间更细粒度的实时反馈仍有扩展空间。

### 10.6 论文与代码依据核验

为避免论文描述脱离真实实现，本文按"产品语境—依赖版本—路由—核心业务代码—数据库证据"五类材料做了交叉核验：

| 核验对象 | 主要依据 | 本文修订口径 |
| --- | --- | --- |
| 产品术语与业务边界 | `CONTEXT.md` | 学生侧统一使用项目、篇目、会话、日常会话归档、挑战、布鲁姆认知路径等术语；教师侧使用教师问答、学习记录核实、AI 预审、确认无误、修订回答 |
| 前端与依赖版本 | `web/package.json` | Next.js 16.2、React 19.2、AI SDK v6、Tailwind v4、Base UI primitives、lucide-react |
| 学生端路由 | `web/src/app/student/**/page.tsx` | `/student` 为学习提问空间，`/student/me` 为学生看板；`/student/projects/*` 与 `/student/challenge/[projectId]` 是重定向兼容入口；不再描述已删除的 `/(student)/cognitive-path/[projectId]` |
| 挑战主流程 | `web/src/app/api/challenge/generate/route.ts`、`evaluate/route.ts`、`components/workbench/challenge-client.tsx`、`lib/data/student.ts` | 当前挑战为"单题面 + guidance"并写入 `practice_records`；评估结果只有通过 / 未通过；状态由挑战记录计算 |
| RAG 与 embedding | `web/src/lib/data/retrieval.ts`、`web/src/app/api/attachments/route.ts`、migration 历史 | 当前会话级 RAG 使用 768 维 embedding，不再沿用旧维度或固定绑定某个 OpenAI embedding model |
| 教师核实与 AI 预审 | `web/src/lib/data/teacher-actions.ts`、`web/src/lib/teacher-pre-review.ts` | AI 预审覆盖完整会话中所有 AI 回答；教师修订先同步学生侧，只有会话级最终提交才物化 SFT/DPO |
| 数据库结构 | Supabase 远程表结构、`database.types.ts`、`web/supabase/migrations/*.sql` | 表数、字段与枚举以已核验材料为准；RLS、触发器、RPC 数量不再作为论文硬性精确断言 |

---

## 第十一章 数据库与系统进一步优化建议

> 本章基于云端 Supabase 表结构、远程 migration 历史、本地 migration 记录与应用代码调用点的交叉审阅，给出可在后续迭代中逐步落地的优化建议。本章不再只是结构性陈述，而是按"问题—建议—实施方式"的结构提出 12 条可执行改动。

### 11.1 索引层优化

#### O1：为教师审阅页增加复合索引

**问题**。教师审阅页的 P95 查询模式是：按 `class_id` + `deleted_at IS NULL` + `source = 'student_chat'` + 最近 `updated_at` 倒序取会话列表，再按 `conversation_id` 取消息。目前云端存在 `conversations_active_owner_idx`，但其前缀是 `owner_id`，在"按班级看所有学生"的教师视图下并不命中。

**建议**。新增条件偏索引：

```sql
create index conversations_active_class_idx
  on public.conversations (class_id, updated_at desc)
  where deleted_at is null and source = 'student_chat';
```

以及用于"待核实会话数"统计的 covering index：

```sql
create index conversations_class_updated_covering_idx
  on public.conversations (class_id, updated_at desc)
  include (owner_id, project_id, title)
  where deleted_at is null and source = 'student_chat';
```

**实施方式**。新增一条 migration `202605110001_teacher_audit_indexes.sql` 并直接在云端 apply。

#### O2：为 `conversation_messages` 按 `(conversation_id, created_at)` 建立索引

**问题**。读取会话详情时必按 `conversation_id` 排序展示，现在该表只有主键 index，按会话查消息需要通过 FK 列的隐式 bitmap scan。

**建议**：

```sql
create index conversation_messages_conversation_created_idx
  on public.conversation_messages (conversation_id, created_at);
```

#### O3：`audit_records` 按班级 + 教师 + 时间的复合索引

**问题**。管理员导出页的"按班级 / 按教师筛选并按时间倒序"是高频查询，但当前只有 `audit_records_exportable_idx(kind, status, updated_at)` 与分离的 `idx_audit_auditor`。

**建议**：

```sql
create index audit_records_class_time_idx
  on public.audit_records (class_id, updated_at desc)
  where status in ('approved','exported');

create index audit_records_auditor_time_idx
  on public.audit_records (auditor_id, updated_at desc)
  where status in ('approved','exported');
```

#### O4：`practice_records` 按项目 + 时间倒序

**问题**。学生个人认知矩阵与项目挑战复盘都需要按项目拉最近的挑战记录。

**建议**：

```sql
create index practice_records_project_created_idx
  on public.practice_records (project_id, created_at desc);

create index practice_records_student_created_idx
  on public.practice_records (student_id, created_at desc);
```

### 11.2 向量检索层优化

#### O5：为 `document_chunks` 加 `conversation_id + chunk_index` 复合索引

**问题**。`match_conversation_document_chunks()` 会先按 `conversation_id` 筛选再按相似度返回，现有 `document_chunks_conversation_id_idx` 只覆盖 `conversation_id`，在排序回表时会再 scan。

**建议**：

```sql
create index document_chunks_conv_chunk_idx
  on public.document_chunks (conversation_id, chunk_index)
  where conversation_id is not null;
```

#### O6：HNSW 参数化

**问题**。当前 HNSW 索引默认参数：

```sql
create index document_chunks_embedding_hnsw
  on public.document_chunks using hnsw (embedding vector_cosine_ops);
```

在教育场景的"多会话小图"下，建议手动调参以平衡 recall 与构建时间：

```sql
drop index if exists document_chunks_embedding_hnsw;
create index document_chunks_embedding_hnsw
  on public.document_chunks using hnsw (embedding vector_cosine_ops)
  with (m = 24, ef_construction = 80);
-- 会话检索时设置 SET LOCAL hnsw.ef_search = 48; 以拉高查询 recall
```

`m=24` 提高图连接度，`ef_construction=80` 提升建图质量；查询侧 `ef_search=48` 是经验值，配合语料规模可再调整。

#### O7：Embedding 维度升级路径预留

**问题**。当前会话级 RAG 使用 768 维 embedding，表字段、附件入库与检索常量都围绕 768 维设计。未来如升级到 3072 维或其他维度，pgvector 定长列无法在同一列内混存不同维度，必须显式设计迁移窗口。

**建议**。新增模型与维度元数据，并采用影子列或新表完成双写迁移：

```sql
alter table public.document_chunks
  add column embedding_model text default 'configured-embedding',
  add column embedding_dimensions integer default 768;

-- 若切换到 3072 维，可新增影子列并重建 HNSW：
alter table public.document_chunks
  add column embedding_v2 extensions.vector(3072);
```

实施时按"新增列 → 后台回填 → 双读灰度 → 切换 RPC → 删除旧索引"推进，避免直接在生产列上破坏现有检索。

### 11.3 事件表与审计层优化

#### O8：`data_quality_events` 加严约束 + 分区

**问题**。当前该表只有 `id, event_type, table_name, record_count, reason, payload jsonb, created_at`，`event_type` 是自由 text，容易出现命名漂移（如 `schema_drift` vs `schemaDrift`）。

**建议**：

```sql
create type public.data_quality_event_type as enum (
  'schema_drift',
  'finalization_payload_invalid',
  'export_skipped',
  'mcp_health_failed',
  'provider_health_failed',
  'rls_bypass_attempt'
);

alter table public.data_quality_events
  alter column event_type type public.data_quality_event_type
  using event_type::public.data_quality_event_type;

create index data_quality_events_time_idx
  on public.data_quality_events (event_type, created_at desc);
```

如预计事件量增长显著，可再引入按月分区：

```sql
-- 伪代码：pg_partman 或 declarative partitioning
create table public.data_quality_events_2026_05 partition of data_quality_events
  for values from ('2026-05-01') to ('2026-06-01');
```

#### O9：`export_batches` JSONL 外部化

**问题**。`export_batches.jsonl` 为 `text`，大样本（> 10k 条）会造成单行过长，影响备份与复制延迟。

**建议**。改用 Supabase Storage：

```sql
alter table public.export_batches
  add column storage_bucket text,
  add column storage_path text,
  add column byte_size bigint,
  add column checksum text;

-- 保留 jsonl 字段用于小批次回退，大批次落 Storage
```

迁移策略：新导出直接落 Storage，存 `storage_bucket + storage_path + checksum`；旧数据保留 `jsonl` 字段不动。

#### O10：为 `audit_records` 增加会话级聚合物化视图

**问题**。教师审阅页需要展示"该会话的待核实回答数 / 风险回答数 / SFT 产出数"，每次都从 `audit_records` 做聚合。

**建议**。新增物化视图：

```sql
create materialized view public.audit_summary_by_conversation as
select
  source_conversation_id as conversation_id,
  count(*) filter (where kind = 'sft') as sft_count,
  count(*) filter (where kind = 'dpo') as dpo_count,
  count(*) filter (where status = 'pending') as pending_count,
  max(updated_at) as last_reviewed_at
from public.audit_records
group by source_conversation_id;

create unique index audit_summary_by_conversation_pk
  on public.audit_summary_by_conversation (conversation_id);
```

并在 `audit_records` 的 AFTER INSERT/UPDATE 触发器里做增量刷新（或定时 `REFRESH MATERIALIZED VIEW CONCURRENTLY`）。

### 11.4 业务契约层优化

#### O11：`conversations.deleted_at` 与导出样本一致性触发器

**问题**。当前允许学生先提交会话级最终提交、之后再删除会话；删除后教师侧不应再看到该会话，但已形成的 `audit_records` 依然存在。这一语义已经在 CONTEXT.md 中定义，但没有触发器兜底。

**建议**。新增 AFTER UPDATE 触发器：

```sql
create function public.on_conversation_soft_delete()
returns trigger language plpgsql as $$
begin
  if new.deleted_at is not null and old.deleted_at is null then
    -- 已进入可导出样本的 audit_records 保留不变
    -- 未达终态的 pending 审阅跳过
    update public.audit_records
      set status = 'rejected',
          rationale = coalesce(rationale, '') || ' [conversation soft-deleted]'
      where source_conversation_id = new.id
        and status = 'pending';
  end if;
  return new;
end; $$;

create trigger conversations_on_soft_delete
  after update on public.conversations
  for each row execute function public.on_conversation_soft_delete();
```

这条触发器把"会话软删除后挂起的审阅自动进入 `rejected`"的产品语义固化，避免教师在学生删除后仍看到该会话的待办。

#### O12：`text_projects` 空项目状态的显式标记

**问题**。项目下所有会话被删除后，项目成为"空项目"，但目前只能通过 JOIN 查询发现。`classification_state` 枚举不包含"空项目"，前端需要在应用层判断。

**建议**。扩展 `classification_state`：

```sql
alter type public."text_project_classification_state_t" add value 'empty';
-- (若原为 text check 约束，则改为)
alter table public.text_projects
  drop constraint text_projects_classification_state_check;
alter table public.text_projects
  add constraint text_projects_classification_state_check
    check (classification_state in ('pending','classified','failed','manual','empty'));
```

并在"最后一条未删除会话被软删除"时，通过触发器把项目切为 `empty`。

### 11.5 小结

上述 12 条建议中，以下已在本次迭代中直接落地到云端真源：

**已实施（3 条 migration）**：
- O1 / O2 / O3 / O4 / O5：全部索引已通过 `20260511000001_db_optimization_phase1.sql` 创建
- 冗余 RLS policy 清理（6 条旧策略已删除）
- `idx_audit_status` 已删除
- 4 个函数 `search_path` 已修复
- 13 个 trigger 函数 + 7 个 RLS helper + 6 个 admin RPC 的 EXECUTE 权限已收紧（`20260511000002_security_definer_lockdown.sql`）
- CONTEXT.md 规则缺口修复：`validate_conversation_message_contract` 已增加 finalized 会话阻止学生续问 + 软删除会话阻止插入（`20260511060200_block_messages_in_finalized_conversations.sql`）

**保留为后续迭代建议**（O6 / O7 / O8 / O9 / O10 / O11 / O12）：
- O6（HNSW 参数调优）：需要在更大语料量下验证 recall 收益
- O7（Embedding 维度升级路径）：需要配合模型切换计划
- O8（`data_quality_events` 枚举化）：需要应用层配合
- O9（`export_batches` JSONL 外部化到 Storage）：需要前端下载逻辑配合
- O10（审阅聚合物化视图）：需要评估刷新频率与一致性
- O11（会话软删除后挂起审阅自动 reject）：需要端到端回归测试
- O12（`text_projects` 增加 `empty` 状态）：需要前端配合

所有建议都必须通过 `web/supabase/migrations/*.sql` 提交并应用到云端唯一真源，不应使用"先改线上、再补 migration"的反模式。

---

## 第十二章 总结与展望

### 12.1 工作总结

本文围绕"古诗文 AI 教学"这一具体场景，设计并实现了一个面向学生、教师、管理员三类角色的 AI 教学工作台——文韵智途。论文主要完成了以下工作：

1. **问题定义**。从古诗文教学的实际困境出发，识别出学习过程碎片化、学习结果难以确认、AI 回答可信性不足、教学数据难以高质量沉淀四类核心问题；
2. **业务闭环设计**。提出"学生提问—项目沉淀—挑战确认—教师核实—管理员治理"五步闭环，明确各角色职责与系统边界；
3. **架构设计**。构建 Next.js 16 + Supabase 全栈架构，包含表现、业务交互、AI 能力、数据服务、运维治理五层结构；
4. **数据库实现**。以云端 Supabase 实例为唯一真源，落地 18 张业务表、7 种核心业务枚举，以及围绕 RLS、契约触发器、RPC / helper 函数组织的数据库规则；
5. **核心功能实现**。完成学生提问与项目归属、单题面挑战生成与评估、教师学习记录核实、AI 预审、会话级最终提交、SFT/DPO 样本物化与导出等主要功能；
6. **治理能力**。通过 Provider / Capability / Tier / Scenario 四维绑定把 AI 能力治理从代码里剥离到管理员后台，使平台具备无代码切换 Provider 与模型的能力；
7. **优化建议**。基于云端表结构、migration 历史与代码调用点的交叉审阅，提出涵盖索引、向量检索、事件表、导出、物化视图、业务契约等 12 条可执行的进一步优化建议。

### 12.2 创新点

- **C1**：把古诗文学习从"零散问答"上升为"围绕篇目的项目化学习单元"；
- **C2**：用"挑战确认"替代"会话推断"，使认知评价与学习过程严格分离；
- **C3**：把教师核实纳入 AI 学习闭环主流程，建立可信教学数据链路；
- **C4**：通过 Provider / Capability / Tier / Scenario 分层绑定把 AI 能力治理从代码中解耦；
- **C5**：把产品规则（学生单班、项目不可删、会话归属、审阅契约）通过 RLS 策略与触发器固化到数据库层，实现"规则下沉"的架构模式。

### 12.3 未来工作

1. **更细颗粒的教师分析**。在教师看板中引入挑战通过率趋势、错题聚类、高频提问分析等；
2. **面向古诗文场景的专用检索**。在 RAG 层引入字义、典故、意象等结构化语义索引，改进在文言文上的召回质量；
3. **长周期学习档案**。为学生在跨学期、跨教师、跨班级维度建立长周期学习画像，与挑战结果联动；
4. **更完善的导出治理**。实现基于 Storage 的大批次导出、checksum 校验、导出水印与审计链；
5. **教师端协作**。引入跨教师的审阅协同、交叉审阅、争议复核机制；
6. **课堂即时反馈**。在课堂教学场景中引入基于 Realtime 的即时提问与反馈通道；
7. **国际化**。支持繁体中文、英文等界面语言，并探索面向英语古典文本的场景扩展。

### 12.4 结语

本文所做的工作并不是简单地"给古诗文教学加一个 AI 会话入口"，而是尝试回答一个更基本的问题：**AI 如何在古诗文教学中被合理组织、可信使用、持续治理**。从学生的一次提问开始，到教师的每一次审阅把关，再到管理员的数据导出与批次治理，系统把 AI、教学过程与数据资产串联成一个可观测、可干预、可演进的整体。这也是本毕业设计最核心的价值所在。

---

## 参考文献

[1] Anderson, L. W., & Krathwohl, D. R. (Eds.). (2001). *A taxonomy for learning, teaching, and assessing: A revision of Bloom's taxonomy of educational objectives*. Longman.

[2] Lewis, P., Perez, E., Piktus, A., et al. (2020). Retrieval-augmented generation for knowledge-intensive NLP tasks. *Advances in Neural Information Processing Systems*, 33, 9459–9474.

[3] Rafailov, R., Sharma, A., Mitchell, E., et al. (2023). Direct preference optimization: Your language model is secretly a reward model. *Advances in Neural Information Processing Systems*, 36.

[4] Malkov, Y. A., & Yashunin, D. A. (2018). Efficient and robust approximate nearest neighbor search using hierarchical navigable small world graphs. *IEEE Transactions on Pattern Analysis and Machine Intelligence*, 42(4), 824–836. https://doi.org/10.1109/TPAMI.2018.2889473

[5] Anthropic. (2024). *Model Context Protocol specification*. https://modelcontextprotocol.io

[6] Vercel. (2026). *AI SDK v6 documentation*. https://ai-sdk.dev

[7] Supabase. (2026). *Supabase documentation: PostgreSQL, RLS, pgvector, Realtime*. https://supabase.com/docs

[8] Vercel. (2026). *Next.js 16 App Router documentation*. https://nextjs.org/docs

[9] OpenAI. (2024). *Embeddings guide*. https://platform.openai.com/docs/guides/embeddings

[10] 教育部. (2022). *义务教育课程方案和语文课程标准（2022 年版）*. 北京: 北京师范大学出版社.

[11] 朱光潜. (1984). *诗论*. 生活・读书・新知三联书店.

[12] 中华人民共和国教育部. (2020). *普通高中语文课程标准（2017 年版 2020 年修订）*. 北京: 人民教育出版社.

[13] Ouyang, L., Wu, J., Jiang, X., et al. (2022). Training language models to follow instructions with human feedback. *Advances in Neural Information Processing Systems*, 35.

[14] Vaswani, A., Shazeer, N., Parmar, N., et al. (2017). Attention is all you need. *Advances in Neural Information Processing Systems*, 30.

[15] Wei, J., Wang, X., Schuurmans, D., et al. (2022). Chain-of-thought prompting elicits reasoning in large language models. *Advances in Neural Information Processing Systems*, 35.

[16] PostgreSQL Global Development Group. (2026). *PostgreSQL 17 documentation*. https://www.postgresql.org/docs/17/

[17] VanLehn, K. (2011). The relative effectiveness of human tutoring, intelligent tutoring systems, and other tutoring systems. *Educational Psychologist*, 46(4), 197–221.

[18] Piech, C., Bassen, J., Huang, J., et al. (2015). Deep knowledge tracing. *Advances in Neural Information Processing Systems*, 28.

[19] Krathwohl, D. R. (2002). A revision of Bloom's taxonomy: An overview. *Theory Into Practice*, 41(4), 212–218.

---

## 附录

### 附录 A 数据库核心 ENUM 清单

| ENUM | 取值 |
| --- | --- |
| `app_role` | `admin`, `teacher`, `student` |
| `audit_kind` | `sft`, `dpo`, `metadata` |
| `audit_status` | `pending`, `approved`, `rejected`, `exported` |
| `export_status` | `queued`, `ready`, `failed` |
| `interaction_source` | `student_chat`, `teacher_chat`, `practice` |
| `prompt_preset_status` | `draft`, `published`, `disabled` |
| `provider_capability` | `student_chat`, `teacher_chat`, `bloom_classification`, `project_classification`, `practice_generation`, `practice_evaluation`, `audit_assist`, `embedding` |

### 附录 B 系统 RPC 清单

| 函数 | 参数 | 返回 |
| --- | --- | --- |
| `authenticate_school_account` | `p_login_id text, p_password text` | `table(id, login_id, role, display_name)` |
| `current_app_user_id` | 无 | `uuid` |
| `current_profile_role` | 无 | `app_role` |
| `get_model_tier_provider` | `p_tier text` | table |
| `get_provider_capability_provider` | `p_capability provider_capability` | table |
| `is_admin` | 无 | `boolean` |
| `is_student_conversation_finalized` | `p_conversation_id uuid` | `boolean` |
| `match_conversation_document_chunks` | `query_embedding, conversation_id, match_count, match_threshold` | table |
| `match_document_chunks` | `query_embedding, match_count, match_threshold, project_id` | table |
| `refresh_project_highest_bloom_level` | `p_project_id uuid` | void |
| `save_model_tier_binding_and_sync` | `p_tier, p_provider_id, p_model_id` | void |
| `save_scenario_tier_bindings_and_sync` | `p_bindings jsonb` | void |
| `rebuild_scenario_provider_capabilities` | 无 | void |
| `teacher_can_access_class` | `p_class_id uuid` | `boolean` |
| `has_valid_app_session_signature` | 无 | `boolean` |

### 附录 C 学号 / 工号格式

- **学号**：8 位数字 = 入学年份 4 位 + 班号 2 位 + 流水号 2 位；
- **工号**：8 位数字 = 入职年份 4 位 + 流水号 4 位。

由 `src/lib/school-login.ts` 中的正则 `/^\d{8}$/` 与 `describeSchoolLoginId()` 完成解析。

### 附录 D 软件技术栈清单

| 层 | 技术 |
| --- | --- |
| 语言 | TypeScript 5.x / SQL / PL/pgSQL |
| 前端框架 | Next.js 16.2 + React 19.2 + App Router |
| UI | shadcn 风格组件 + Base UI primitives + Tailwind CSS v4 + lucide-react |
| 状态管理 | React Server Components + Server Actions |
| AI | Vercel AI SDK v6 + @ai-sdk/openai + @ai-sdk/mcp |
| 数据库 | Supabase (PostgreSQL 17) + pgvector + HNSW |
| 身份 | 学校账号自定义登录 + 自定义会话签名 + RLS |
| 构建 | Next.js built-in + ESLint 9 |
| 测试 | Node `--test --experimental-strip-types` |
| 运行时 | Node.js 20+ |

---

## 致谢

感谢指导教师在课题选题、系统设计与论文撰写过程中给予的耐心指导与宝贵建议。感谢在系统测试阶段参与体验与反馈的教师与同学。感谢家人在整个毕业设计过程中的支持。感谢开源社区，尤其是 Next.js、Supabase、pgvector、Vercel AI SDK、shadcn 生态等项目的贡献者，没有这些高质量的开源工具，这项工作在一个学期的时间内是难以完成的。

同时感谢我在项目开发中使用到的 AI 编程助手在代码实现与文档梳理方面提供的辅助。在本文中，所有与架构决策、数据库设计、业务闭环相关的内容均由本人依据实际系统与 CONTEXT 规范核对确认。
