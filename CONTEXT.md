# 文韵智途产品语境

文韵智途是面向古诗文/文言文教学的 AI 学习工作台，核心价值是在学生学习、教师教学与管理员运营之间形成可信的 AI 教学闭环。

## Language

**项目**:
一个学生围绕单个古诗文篇目沉淀出的学习单元，可由学生先提问再被系统项目化归属。
_Avoid_: 课程、任务、作品集

**篇目**:
学生学习的古诗文文本名称，是创建项目的依据。
_Avoid_: 文章、素材、文档

**会话**:
项目下的一次学生与 AI 的对话学习记录；无法识别篇目的学生提问保存为日常会话归档下的会话。学生侧称会话，不称学生问答。
_Avoid_: 聊天、问答、conversation

**日常会话归档**:
学生提问无法可靠归属到具体篇目时使用的兜底归档容器，用于保存和回看普通会话记录，但不属于篇目项目。
_Avoid_: 日常对话项目、临时项目、未归属项目

**布鲁姆认知分布**:
学生看板中基于挑战确认结果形成的项目层面认知统计。
_Avoid_: 学习进度、能力画像、等级分布

**布鲁姆认知路径**:
单个会话结束后，根据该会话中的学生问题确定出的布鲁姆认知层级变化轨迹。
_Avoid_: 对话路径、学习轨迹、认知等级

**挑战**:
用于真实确认学生认知水平的分层任务，必须区别于普通对话。
_Avoid_: 测验、练习、考试

**教师问答**:
教师面向备课、教学处理和教学判断的 AI 交互。
_Avoid_: AI 聊天、教师聊天

**教学正确性核实**:
教师对学生 AI 学习过程与输出进行可信性检查的活动，教师侧应表达为待核实学习记录而不是数据打标。
_Avoid_: 审核、批改、审计、打标

**AI 预审**:
教师审阅学生会话前，由 AI 对会话中疑似误导或错误的片段进行预先提示。
_Avoid_: 自动判分、机器审核

**标红片段**:
AI 预审后在学生会话中突出显示的疑似问题位置，供教师快速定位。
_Avoid_: 错题、异常日志

**确认无误**:
教师确认某条 AI 回答无需修订且可进入教学数据导出的操作。
_Avoid_: 审核通过、标注完成

**修订回答**:
教师直接编辑学生会话中的单条 AI 回答，以形成更正确的教学回答。
_Avoid_: 改标、纠错数据

**教师修订提示**:
学生侧看到教师修改过会话记录时出现的明确提示，说明该回答已由教师修订。
_Avoid_: 系统通知、后台修改提示

**AI Native 后台**:
管理员用于配置、诊断、观测和导出 AI 系统能力的专业操作界面。
_Avoid_: 普通管理后台、系统设置

**看板**:
学生、教师、管理员进入各自角色后的首页概览。
_Avoid_: 首页、Dashboard、工作台

## Relationships

- 一个 **项目** 对应一个 **篇目**。
- 学生 **项目** 不支持删除，以保持篇目学习单元、挑战结果和教师核实关系稳定。项目下所有会话都删除后，项目仍保留为空项目状态：篇目名保留，提示“暂无会话，可继续提问”；历史已完成挑战结果保留，没有挑战结果时显示“等待挑战确认”。
- 学生 **会话** 和 **教师问答** 都支持删除，产品语义是删除，数据实现语义按软删除设计。学生侧删除入口放在会话列表项的更多菜单中；教师侧删除入口放在 **教师问答** 历史列表项的更多菜单中；删除前都需要确认，并说明删除影响范围。第一阶段不提供删除撤销入口。学生删除会话时，若会话内 AI 回答尚未被教师 **确认无误** 或 **修订回答**，删除后不再进入教师待核实，也不进入布鲁姆或挑战依据；该会话中的学生问题不再参与未来 **布鲁姆认知路径** 展示或 **挑战** 生成，基于这些问题生成的布鲁姆层次统计与路径也随之消失，但已经完成的挑战结果不回滚。若已形成可导出样本，学生侧删除只隐藏/移除会话展示，后台保留已形成的 SFT/DPO 数据和审阅元数据。教师删除 **教师问答** 只影响教师侧历史记录，不影响学生项目、学生会话、学习记录核实、SFT/DPO 导出或挑战。
- 学生可以先发起 **会话**，系统应自动将该会话归属到一个 **项目**，不要求学生手动确认。
- 只有当系统能明确识别具体 **篇目**，或能从当前项目/当前会话上下文继承明确 **篇目** 时，才将会话归入 **项目**；系统不做猜测式归属，也不弹窗要求学生确认。学生从具体项目入口提问时，当前项目上下文不是强绑定；如果学生问题明确指向另一个 **篇目**，应优先归入或迁入该新篇目项目，并提示“已归入《篇目》项目”。
- 无法可靠识别 **篇目** 或归属判断失败的学生提问应保存到 **日常会话归档**，不归入 **项目**。
- **日常会话归档** 中的会话如果在同一会话后续明确出现 **篇目**，应随当前会话整体自动迁入对应 **项目**；跨会话不追溯迁移历史日常归档记录。已归入篇目 **项目** 的会话中，如果学生明确转问另一个 **篇目**，应从转问处自动拆成新会话并归入新篇目项目，原会话保留在原项目，不做项目间整条迁移。自动拆新会话不打断学生提问，只轻提示“已开始《篇目》新会话”，并在侧边栏展开高亮新项目/新会话。
- 从 **日常会话归档** 迁入 **项目** 的会话，只从 **篇目** 明确后的上下文开始生成或更新 **布鲁姆认知路径**；迁入前内容保留在会话记录中，但不参与布鲁姆判断或挑战生成。
- **日常会话归档** 可供学生正常回看，但第一阶段教师侧完全不展示，不进入教师 **教学正确性核实**、不生成 **布鲁姆认知路径** 或 **布鲁姆认知分布**，也不生成 **挑战**。删除 **日常会话归档** 中的会话只从学生侧归档列表移除，不影响其他链路。
- 界面必须让学生感知到提问后已经进入 **项目** 层级，而不是停留在纯聊天体验。
- 学生从导航进入“学习提问”时，第一屏始终从空白提问状态开始，不默认选中最近项目；只有从具体项目或会话入口进入时，才显示对应项目上下文。空白提问状态主文案引导学生直接问古诗文问题，辅助提示说明提到具体篇目时会自动归入项目，不能把“输入篇目”作为主行动。
- 学生提问时如果 AI 调用失败，学生消息先保存；若能从文本规则明确判断 **篇目**，先归入对应 **项目**，否则进入 **日常会话归档**。AI 回答失败状态在会话中可重试显示，不生成 **布鲁姆认知路径**、不进入教师待核实、不进入挑战依据；重试成功后再按正常流程补齐 AI 回答、布鲁姆认知路径和教师待核实。
- 自动归属 **项目** 的反馈应保持轻量：只在新会话首次归入项目或 **日常会话归档** 迁入项目时提示一次“已归入《篇目》项目”，之后通过侧边栏高亮和项目标题维持上下文，不每轮重复提示。
- 一个 **项目** 包含一个或多个 **会话**。
- 后续 **挑战** 应基于 **项目** 维度，结合学生在该项目下过往 **会话** 中提出的问题设计攀升路径；挑战生成不等待教师核实，但必须排除 **日常会话归档**。
- **挑战** 生成提示词保持简单，只输入 **篇目**、项目下学生问题列表和当前挑战层级；不输入完整 AI 回答、教师修订内容或 SFT/DPO 审阅数据。项目下学生问题较少时仍可生成挑战，系统可以围绕篇目和当前挑战层级自主补足出题。挑战题型不固定为单一类型，应在提示词中给出可选题型范围，由模型根据篇目、学生问题和当前挑战层级自主选择合适题型，但必须符合挑战用于确认认知水平的原则。
- 一个 **会话** 产生一条 **布鲁姆认知路径**，其判断依据是该会话中的学生问题，不依据 AI 原回答或教师修订回答。
- 一个 **项目** 汇总下属 **会话** 形成 **布鲁姆认知分布**。
- **学生提问空间** 以 **项目** 优先的侧边栏组织会话。
- **日常会话归档** 在学生提问空间侧边栏中作为项目列表之后的固定弱入口呈现，只展示会话列表和最近更新时间，不显示布鲁姆徽章或挑战入口。
- **挑战** 失败或表现低于当前项目认知水平时，不即时降低已确认的最高层级；先标记该层级“待巩固”并引导学生回到项目会话继续学习，连续多次同层级失败时项目状态显示为“需要巩固”。
- **挑战** 入口页展示项目卡片墙，单个项目挑战页展示从低到高的认知攀登路线图。
- **挑战** 结果面向学生展示简短反馈：是否通过、对应布鲁姆层级、1-2 条改进建议，以及“继续挑战/回到项目提问”等下一步行动；不展示模型完整评价过程、长评分细则或后台判定 JSON。
- **布鲁姆认知分布** 主要出现在学生 **看板** 中，且主统计只使用 **挑战** 确认后的结果；会话推断出的 **布鲁姆认知路径** 只作为项目/会话内轻量反馈，不进入看板主统计。没有挑战结果的项目在看板中显示“等待挑战确认”。
- 学生侧项目卡片、挑战卡片和相关认知状态直接显示布鲁姆术语，不用另造学生友好的替代表达；挑战页显示完整六层攀登路线，学生看板和项目卡片只显示当前确认层级、下一挑战层级和状态。
- 教师 **学习记录核实** 不是平铺队列，而是按 **班级 → 学生 → 项目 → 会话 → 单条 AI 回答** 层级浏览并处理。默认第一屏是班级级总览，不自动钻入某个学生会话；即使教师只负责一个班级，也保留班级层，以单班级卡片和摘要呈现。班级总览显示待核实回答数、风险回答数和最近学习时间。进入班级后，学生层使用列表/表格展示姓名、待核实回答数、风险回答数、涉及项目数、最近学习时间和挑战确认概览，默认按风险回答数优先、最近学习时间倒序排列。进入学生后，项目层只做概览，展示篇目名、待核实回答数、风险回答数、会话数、当前挑战确认层级/状态和最近学习时间，不直接展开所有回答。进入项目后再按会话逐层查看，右侧展示完整会话并在单条 AI 回答气泡上确认或修订。
- 教师 **看板** 聚焦学生认知、学生学习过程纠正和待核实学习记录，不展示备课提醒；学生认知主概览基于 **挑战** 确认结果，不混入会话推断路径。教师进入具体学生/项目/会话核实时，可以看到 **布鲁姆认知路径** 作为参考。所有篇目 **项目** 下的 AI 回答都进入待核实范围，未经教师 **确认无误** 或 **修订回答** 不能视为通过；AI 预审判定有风险的回答优先展示。教师从看板点击“待核实学习记录”入口时进入班级总览并高亮“需优先核实”统计，不直接跳入单条会话；从具体班级或学生卡片进入时，携带对应上下文进入相应层级。
- **教师问答** 是备课与教学处理入口，但不需要进入教师 **看板** 的任务提示；**教师问答** 与 **学习记录核实** 在侧边栏并列，第一阶段不在页面内互相推荐，避免教师把备课问答和学生学习过程核实混成同一流程。
- 教师应按学生、项目和会话审阅完整学习过程，而不是只处理抽象风险摘要。
- 教师界面必须保留多轮 **会话** 上下文用于教学判断，但 **确认无误** 和 **修订回答** 的操作粒度落在单条 AI 回答气泡；每条 AI 回答可处于“待核实 / 确认无误 / 已修订”状态。教师 **确认无误** 后仍可改为 **修订回答**；状态从“确认无误”变为“已修订”，后续导出从仅 SFT 变为 SFT + DPO，已导出样本再次修订后作为“有更新”样本进入新的导出批次。第一阶段不提供批量确认无误功能。
- 教师审阅前先进行 **AI 预审**，并在会话中显示 **标红片段** 以帮助定位疑似误导；AI 预审定位粒度是具体句段，教师处理粒度仍是整条 AI 回答气泡。
- 教师审阅界面采用两栏沉浸布局：左侧保留学生、项目、会话树和会话列表，右侧显示当前完整会话与内联操作。会话列表项显示时间、待核实回答数、风险回答数和 **布鲁姆认知路径** 摘要；右侧完整会话中，AI 回答气泡显示状态和操作，风险句段在气泡内标红。
- 教师执行 **修订回答** 时应直接编辑聊天气泡，而不是进入单独表单；界面允许局部编辑，但保存后按整条修订版 AI 回答存储、同步给学生并用于导出。教师可再次修订同一条回答，学生侧和后续导出始终使用最新修订版；系统保留修订历史但不在教师主界面展开，已导出样本再次修订后作为“有更新”样本进入新的导出批次。
- 学生侧可立即看到 AI 原回答，不因教师尚未核实而阻塞学习；教师完成 **修订回答** 后，学生侧用教师修订版替换原回答并显示 **教师修订提示**。教师 **确认无误** 状态不在学生侧展示。
- 教师 **修订回答** 只影响学生侧显示的回答正文，以及后续 SFT/DPO 导出内容；不影响 **布鲁姆认知路径**，也不要求改变挑战生成提示词。
- 教师 **修订回答** 必须在后台保留原 AI 回答用于 DPO 导出；教师修订版作为 chosen，原 AI 回答作为 rejected。学生侧不展示原 AI 回答。
- 教师侧内部 URL、API 和数据库命名可以暂时保留 audit，但侧边栏入口、按钮、页面标题和提示文案必须使用“学习记录核实 / 待核实学习记录”等教师可理解语言。
- 管理员 **用户管理** MVP 先提供账号可视化、CSV 导入入口、角色/状态/活动信息展示和班级归属入口，不在同一阶段提供在线改角色、停用账号或授权配置。
- 管理员 **用户管理** 页必须显示班级归属摘要：教师显示负责班级数量，学生显示所在班级或未分配状态。
- 管理员 **用户管理** 页必须支持按角色、状态筛选，并支持按姓名或账号全文搜索。
- 管理员 **班级成员管理** 必须支持在班级页通过“成员分配”弹窗编辑教师和学生：查看当前成员、按角色添加账号、移除已有成员，并保持在班级上下文中完成操作。
- 教师账号可以归属多个班级；学生账号在 MVP 中只允许归属一个班级，以保持学情统计和权限边界清晰。
- 管理员将学生加入新班级时应视为 **自动迁班**：系统自动移除该学生原有班级关系，并在成员分配弹窗中提示“学生将从原班级迁入当前班级”。
- 管理员可以移除班级最后一个教师，但界面必须提示“移除后该班级暂无负责教师”，因为教师看板和教学正确性核实范围会受影响。
- 学生单班规则必须由数据库约束或等价强约束防止复发：同一个学生账号不能同时拥有多条 student 班级关系；教师多班不受此限制。
- **AI Native 后台** 面向管理员，不应弱化专业 AI 操作能力。
- 管理员不观测学生原始对话或 **日常会话归档**；管理员只查看经过教师 **确认无误** 或 **修订回答** 后形成的可导出 SFT/DPO 数据与审阅元数据。
- 管理员侧 **教学数据导出** 可以显示数据产出汇总计数，例如可导出 SFT 样本数、可导出 DPO 样本数、待教师核实回答数、教师已处理回答数和最近导出批次，但不提供下钻到学生原始对话的入口。
- 每条经过教师 **确认无误** 或 **修订回答** 的 AI 回答形成一个可导出样本；样本保留该回答之前的同一 **会话** 多轮上下文，不包含该回答之后的后续轮次。**确认无误** 只进入 SFT，使用原 AI 回答作为 assistant 内容，不生成 DPO；**修订回答** 进入 SFT，使用教师修订版，同时生成 DPO，chosen 为教师修订版，rejected 为原 AI 回答。
- 管理员导出的审阅元数据只围绕可导出样本，不包含原始未核实会话列表、**日常会话归档** 或学生原始完整对话浏览入口；字段范围包括样本 ID、学生匿名标识、篇目/项目、会话 ID、AI 回答 ID、教师 ID、状态、时间、是否进入 SFT、是否进入 DPO，以及 DPO chosen/rejected 关联 ID。
- 管理员导出统一使用 JSONL：SFT 每行一个 `{ messages, metadata }` 样本，DPO 每行一个 `{ prompt/messages, chosen, rejected, metadata }` 偏好样本，审阅元数据每行一个样本元信息对象。
- 管理员可以对可导出样本做条件筛选后导出，例如按篇目/项目、时间范围、教师、状态、SFT/DPO 类型筛选；筛选只作用于已由教师 **确认无误** 或 **修订回答** 的样本，不提供原始对话浏览或二次审核流程。默认导出“尚未导出过”的可导出样本，管理员可切换为全部历史样本或指定时间范围；每次导出生成导出批次记录。已导出样本允许再次导出，但必须保留历史导出状态；再次导出生成新的批次记录，不覆盖原批次。
- 管理员侧边栏应分为 **学校管理** 与 **AI 运维** 两组，前者承载用户管理和班级关系，后者承载 Provider、MCP、日志和教学数据导出。
- 管理员侧 **AI 运维** 日志只显示技术事件和聚合状态，例如 Provider、模型、MCP、错误码、耗时、调用状态和导出批次状态；不显示学生提问正文或 AI 回答正文。
- 四个入口页面保持统一现代教育 SaaS 风格，古典元素重点落在学生项目、挑战、篇目内容和登录品牌区。
- 开发计划应先建立最小设计系统和全局布局，再落学生黄金路径，随后完成教师审阅闭环，最后完善管理员双分组后台。

## Example dialogue

> **Dev:** “学生在项目里和 AI 聊完后，能不能直接更新认知水平？”
> **Domain expert:** “不能。会话只能形成布鲁姆认知路径，真正确认认知水平必须通过挑战。”

## Flagged ambiguities

- “聊天”容易混淆学生会话与教师问答；学生侧统一称 **会话**，教师侧称 **教师问答**。
- “SFT/DPO”对教师不可见；面向教师应使用无感流程，面向管理员才呈现专业 AI 数据语义。
