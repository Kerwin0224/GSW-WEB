# 文韵智途产品语境

文韵智途是面向古诗文/文言文教学的 AI 学习工作台，核心价值是在学生学习、教师教学判断与管理员 AI 运维之间形成可信的教学闭环。

本文件是项目领域语言、产品不变量和验收边界的唯一真源；代码、测试、提示词、迁移和界面文案若与本文件冲突，应视为实现漂移，而不是另立规则。视觉风格、组件微交互、开发阶段计划、具体 SQL/文件路径和一般工程术语不写入本文件，除非它们改变领域语义。

## Language

### 学生学习

**篇目**:
学生学习的古诗文文本名称，是创建项目的依据。
_Avoid_: 文章、素材、文档

**项目**:
一个学生围绕单个篇目沉淀出的学习单元，可由学生先提问再被系统自动归属。
_Avoid_: 课程、任务、作品集

**会话**:
一次学生与 AI 的多轮学习记录；学生侧称会话，不称学生问答。
_Avoid_: 聊天、问答、conversation

**日常会话归档**:
学生首问无法可靠归属到具体篇目时使用的兜底会话容器，不属于任何篇目项目。
_Avoid_: 日常对话项目、临时项目、未归属项目

**学生提问空间**:
学生进行学习提问和回看历史的工作区，一级容器只包括项目与日常会话归档。
_Avoid_: 聊天页面、问答中心

**看板**:
学生、教师、管理员进入各自角色后的首页概览。
_Avoid_: 首页、Dashboard、工作台

### 布鲁姆与挑战

**布鲁姆认知路径**:
单个学生问题为真正学懂所需要达到的最高充分布鲁姆层级。
_Avoid_: 对话路径、学习轨迹、认知等级

**会话级布鲁姆统计**:
同一会话内多个学生问题各自布鲁姆认知路径最高层级的统计，每个问题只计入一个层级。
_Avoid_: 项目认知结果、挑战确认结果

**挑战**:
用于真实确认学生当前认知水平的分层任务，必须区别于普通会话。
_Avoid_: 测验、练习、考试

**挑战确认结果**:
学生完成挑战后形成的通过或未通过判断，以及对应的目标布鲁姆层级。
_Avoid_: 普通练习结果、模型评分

**布鲁姆认知分布**:
学生看板或教师看板中基于挑战确认结果形成的项目或群体层面认知统计。
_Avoid_: 学习进度、能力画像、等级分布

**待巩固**:
学生某一挑战层级最近一次未通过但尚未连续两次未通过的学习反馈状态。
_Avoid_: 降级、失败等级

**需要巩固**:
学生同一挑战层级连续两次未通过后的学习反馈状态。
_Avoid_: 风险告警、教师待办

### 教师工作

**教师问答**:
教师面向备课、讲解设计、课堂追问和教学判断的通用 AI 交互。
_Avoid_: AI 聊天、教师聊天

**学习记录核实**:
教师在完整学生学习语境中检查学生 AI 学习过程与输出教学正确性的活动。
_Avoid_: 审核、批改、审计、打标

**AI 预审**:
教师核实学生会话前，由 AI 对完整会话中疑似误导或错误的 AI 回答进行预先提示。
_Avoid_: 自动判分、机器审核、AI 辅助审计

**标红片段**:
AI 预审后在学生会话中突出显示的疑似问题句段，供教师定位疑点。
_Avoid_: 错题、异常日志

**确认无误**:
教师在会话级最终提交时确认未修订的 AI 回答无需修订的操作语义。
_Avoid_: 审核通过、标注完成

**确认提交整个会话**:
教师完成一个会话内所有 AI 回答核实并提交最终结果的会话级动作。
_Avoid_: 单条确认、只确认最新回答

**修订回答**:
教师直接编辑学生会话中的某个 AI 回答气泡，以形成更正确的教学回答。
_Avoid_: 改标、纠错数据

**教师修订提示**:
学生侧看到教师修改过会话记录时出现的明确提示。
_Avoid_: 系统通知、后台修改提示

### 管理与 AI 运维

**AI Native 后台**:
管理员用于配置、诊断、观测和导出 AI 系统能力的专业操作界面。
_Avoid_: 普通管理后台、系统设置

**学校管理**:
管理员维护账号、班级和成员关系的后台能力组。
_Avoid_: 组织后台、用户中心

**AI 运维**:
管理员维护 Provider、MCP、Prompt 预设、运行日志和教学数据导出的后台能力组。
_Avoid_: 系统工具、开发者后台

**教学数据导出**:
管理员导出教师最终核实后形成的 SFT/DPO 样本和审阅元数据的能力。
_Avoid_: 原始对话导出、二次审核

**导出批次**:
一次教学数据导出的可追踪记录。
_Avoid_: 下载记录、任务日志

**运行日志**:
管理员用于排查 Provider、API、MCP、导出和渲染错误的技术事件记录。
_Avoid_: 学生原始对话、数据质量事件表

## Relationships

### 唯一真源边界

- `CONTEXT.md` 只承载领域语言和产品不变量；具体页面布局、按钮显隐、代码文件名、SQL 细节和阶段计划必须沉淀到实现文档、测试或 ADR，不能在这里重复扩写。
- 云端 Supabase 是 **业务数据真源**；Supabase 迁移文件是 schema-as-code 记录，不能因为云端是真源而删除或绕过迁移历史。
- 本地 Supabase 只能作为隔离 schema 演练环境，不能作为学生、教师或管理员功能验收口径。

### 学生学习关系

- 一个 **项目** 对应一个 **篇目**；同一个学生对同一个 **篇目** 始终只有一个 **项目**。
- **项目** 不支持学生删除；**项目** 下所有 **会话** 删除后，**项目** 仍保留 **篇目**、**挑战确认结果** 和空项目状态。
- 学生 **会话** 和 **教师问答** 支持删除，产品语义是删除，数据实现语义按软删除处理。
- 学生删除的 **会话** 不得再出现在学生或教师业务查询中，也不得继续进入 **学习记录核实**、**布鲁姆认知路径** 或 **挑战** 依据；已形成导出样本的后台数据是合规例外，必须保留审阅与导出语义。
- 只有从 **学生提问空间** 全局空白入口发起的新 **会话** 首问才调用 **篇目** 识别；从 **项目** 入口或既有 **会话** 继续提问时，必须继承原容器并跳过 **篇目** 识别。
- 首问提到多个 **篇目** 时，**篇目** 识别只能裁决一个主 **篇目**；无法可靠裁决时进入 **日常会话归档**。
- **日常会话归档** 中的 **会话** 可以回看和续问，但不补归属到 **项目**，不进入 **学习记录核实**，不生成 **布鲁姆认知路径**、**布鲁姆认知分布** 或 **挑战**。
- 学生 **看板** 展示 **项目** 概览和 **挑战确认结果**，不展示 **日常会话归档** 入口，不把项目卡片做成项目详情页。
- 学生从 **项目** 卡片进入 **学生提问空间** 时，默认进入该 **项目** 的新 **会话** 空白状态；项目详情页和项目挑战详情页都不是独立主入口。

### 布鲁姆与挑战关系

- 一个 **会话** 包含多个学生问题；每个学生问题至多形成一条 **布鲁姆认知路径**。
- **布鲁姆认知路径** 只依据学生问题本身，不依据 AI 原回答、**修订回答**、**教师问答** 历史或 SFT/DPO 数据。
- **会话级布鲁姆统计** 只帮助后续 **挑战** 取材、选择切入角度和调整表达，不直接更新项目级 **布鲁姆认知分布**。
- 项目级 **布鲁姆认知分布** 只来自 **挑战确认结果**；普通 **会话** 推断不能直接确认 **项目** 认知水平。
- **挑战** 基于 **项目** 生成，输入边界是 **篇目**、当前目标层级和 **项目** 下未删除学生问题；实现可使用有限问题窗口，但必须排除 **日常会话归档**、已删除 **会话**、AI 回答、**修订回答** 和后台审阅数据。
- 同一学生同一项目在任意时刻只能存在一条 **挑战** 记录处于待作答状态；生成新 **挑战** 前必须将该项目下所有已存在的待作答 **挑战** 标记为 `blocked`，再插入新记录，不允许累积多条待作答记录。该不变量由数据库部分唯一索引强制，应用层并发请求会被直接拒绝。
- **挑战** 主流程不向学生提供任意层级选择；当前目标层级应由 **项目** 已确认最高层级推导，不能因学生问题统计而跳级确认。
- **挑战** 评价第一阶段只有通过和未通过；通过只确认当前目标层级，未通过不回滚已确认最高层级。
- 同一层级一次未通过显示 **待巩固**，连续两次未通过显示 **需要巩固**；这两个状态只作为学生侧学习反馈，不进入教师待核实优先级。
- 学生通过创造层级后，**项目** 状态使用“已完成六层认知挑战确认”，不再生成更高层级 **挑战**。
- **挑战** 入口页承载 **项目** 筛选、路线图、生成、作答和提交流程，不重复学生 **看板** 的项目卡片墙。

### 教师工作关系

- 教师侧只有三个一级工作区：教师 **看板**、**教师问答**、**学习记录核实**；独立数据分析页和独立提示词模板管理页不属于教师主工作区。
- 教师 **看板** 只呈现班级整体、优先关注对象、高风险 **项目** 和待核实积压，点击后进入 **学习记录核实**，不在 **看板** 内完成 **确认无误** 或 **修订回答**。
- **教师问答** 只服务教师个人通用 AI 交互；其历史和提示词模板不进入 **学习记录核实**、**挑战** 或 **教学数据导出**。
- **学习记录核实** 必须保留班级、学生、**项目**、**会话** 的完整上下文，不能退化为单条回答待办队列。
- 教师核实对象是整个 **会话** 中所有 AI 回答；最终提交是会话级动作，不提供只确认最新回答或逐气泡最终确认流程。
- 教师 **确认提交整个会话** 后，该 **会话** 进入已核实状态，学生不能在该 **会话** 继续追问。
- **AI 预审** 对象是完整 **会话** 中所有学生提问和 AI 回答；预审结果必须能区分“已覆盖且无明显疑点”和“未覆盖”。
- **AI 预审** 的 **标红片段** 只定位疑点，不代表系统已经判错；教师处理粒度仍是整条 AI 回答气泡和整个 **会话**。
- **修订回答** 在最终提交前是草稿修订：学生侧可看到最新修订版，但不会立即生成 SFT/DPO 样本。
- **确认提交整个会话** 后，未修订回答进入 SFT，**修订回答** 进入 SFT 并生成 DPO，其中 chosen 为教师修订版，rejected 为原 AI 回答。
- 教师侧内部 URL、API 和数据库命名可以暂时保留 audit；教师可见入口、标题、按钮和说明应使用 **学习记录核实** 与 **AI 预审**。

### 管理与导出关系

- **AI Native 后台** 面向管理员，不弱化 Provider、MCP、Prompt 预设、**运行日志** 和 **教学数据导出** 的专业操作能力。
- 管理员侧边栏分为 **学校管理** 和 **AI 运维**；**学校管理** 承载用户与班级，**AI 运维** 承载 Provider、MCP、Prompt 预设、**运行日志** 和 **教学数据导出**。
- 教师账号可以归属多个班级；学生账号在 MVP 中只能归属一个班级。
- 管理员将学生加入新班级时是自动迁班；系统必须移除学生原有班级关系并提示迁班影响。迁班时同步更新该学生所有未删除 **项目** 和 **会话** 的班级归属到新班，使新班教师可见完整学习记录核实历史。
- 管理员可移除班级最后一个教师，但必须提示该班级将暂无负责教师。
- 管理员不浏览学生原始对话或 **日常会话归档**；管理员只查看教师最终核实后形成的可导出样本、**导出批次** 和审阅元数据。
- **教学数据导出** 统一使用 JSONL；SFT 每行一个 `{ messages, metadata }`，DPO 每行一个 `{ prompt/messages, chosen, rejected, metadata }`。
- 后台教师审阅样本的状态语义只有“已审批等待导出”和“已导出”两个终态；不存在“待教师批准”等中间态——**确认提交整个会话** 本身就是审批动作，未发生该动作的回答不进入后台样本。
- **运行日志** 只显示技术事件和聚合状态，例如 Provider、模型、MCP、错误码、耗时、调用状态和 **导出批次** 状态；不得显示学生提问正文或 AI 回答正文。

## Example dialogue

> **Dev:** “学生在项目里和 AI 聊完后，能不能直接更新项目认知水平？”
> **Domain expert:** “不能。会话只能形成布鲁姆认知路径和会话级统计，真正确认项目认知水平必须通过挑战。”

> **Dev:** “教师改了一条 AI 回答后，后台是不是立刻生成 DPO？”
> **Domain expert:** “不能。修订回答只是草稿修订，只有确认提交整个会话后才形成可导出 SFT/DPO 样本。”

## Flagged ambiguities

- “聊天”容易混淆学生会话与教师问答；学生侧统一称会话，教师侧统一称教师问答。
- “审计”和“AI 辅助审计”容易让教师误解为后台审核；面向教师的产品语言统一使用学习记录核实和 AI 预审，audit 只允许作为内部实现命名过渡。
- “布鲁姆认知路径”“会话级布鲁姆统计”“挑战确认结果”“布鲁姆认知分布”不能混用；路径属于单个学生问题，统计属于单个会话，确认结果来自挑战，分布来自确认结果汇总。
- “待巩固 / 需要巩固”不是教师侧风险队列依据；教师优先级来自待核实回答数、AI 预审疑点和最近学习时间。
- “运行日志”不是 `data_quality_events` 的同义词；当前领域语义只允许管理员查看技术事件摘要和聚合状态，不允许把学生原始对话当作日志内容展示。
- “四个入口页面”和“开发计划”不属于领域语言；这类视觉和排期信息应迁出到设计文档、任务计划或 ADR。
- 当前实现仍需与本文件对齐的漂移：部分教师可见文案仍出现“AI 辅助审计”。
