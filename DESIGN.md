# 文韵智途前端设计规范

## 1. 产品气质与角色

以宣纸底色、黛蓝操作色和清晰的中文排版延续古诗文品牌。文化表达集中在登录页与篇目内容，工作页面优先展示任务、对象和状态。三端共用组件，通过信息密度、术语与行动顺序体现角色差异。

| 场景 | 用户需要立刻回答的问题 | 首要内容 | 语言与密度 |
| --- | --- | --- | --- |
| 登录 / 落地页 | 这是什么？如何进入？ | 品牌、用途、学校账号登录 | 一句价值说明，移动端先表单；不展示后台功能目录 |
| 学生 | 我想问什么？下一步练什么？ | 提问框、篇目、继续学习、练习 | 直接、鼓励但不夸大；不展示运行时、SFT/DPO、模型路由等术语 |
| 教师 | 哪些内容需要我处理？ | 待审核记录、班级学情、备课问答 | 教学任务语言；认知层级可以出现，训练流水线不占首屏 |
| 管理员 | 哪个功能出了问题、影响谁、如何处理？ | 功能执行记录、异常处置、账号与班级、服务配置 | 校方操作语言优先；协议、标识符和诊断上下文放在配置字段或展开详情中 |

教师审核是对 AI 回答、标签及教学适用性的核验，不对学生提问作惩罚性评价。学生的“提问次数”“最高挑战层级”均不能命名为掌握率。配置启用不代表运行健康。

## 2. 色彩

唯一实现来源为 `web/src/app/globals.css` 的语义变量；组件使用 Tailwind 语义色，禁止再套 `hsl(var(...))`，因为变量已包含完整 OKLCH 值。

| 用途 | Token | 浅色 | 深色 |
| --- | --- | --- | --- |
| 页面 | `--background` | 宣纸 `#FAF8F1` | 墨灰 `#1A1A2E` |
| 正文 | `--foreground` | `#2D2D2D` | `#FAF8F1` |
| 内容面 | `--card` | 白 | 深墨灰 |
| 主要操作、焦点 | `--primary` / `--ring` | 黛蓝 `#4A6FA5` | 月白 `#7EA3CC` |
| 辅助文字 | `--muted-foreground` | 深暖灰 `#69645E` | 现有浅暖灰 |
| 错误、破坏操作 | `--destructive` | 朱砂 | 亮朱 |
| 认知层级图例 | `--bloom-1` 至 `--bloom-6` | 现有六层色谱 | 现有提亮色谱 |

角色标识采用淡底加文字，不用红色暗示管理员危险；图表、状态不能仅靠颜色区分。金色仅作品牌点缀。

## 3. 字体与文案

品牌与古诗文标题使用现有 `font-heading` 宋体栈；操作页标题、表单、数据使用 `font-sans`；模型 ID、协议、JSON 字段使用 `font-mono`。

| 层级 | 尺寸 | 使用规则 |
| --- | --- | --- |
| 登录展示标题 | 36 / 48 / 60px | 桌面最多两行，手机缩短 |
| 页面 H1 | 24 / 30px | 名词短语，通常 4–10 个汉字 |
| 区块 H2 | 18 / 20px | 说明区块对象，不再重复页头 |
| 正文 | 14 / 16px | 行高 1.5–1.75；长文阅读保持宽松 |
| 辅助文字 | 12px | 只承载非关键元数据；不使用 10px 解释操作 |
| 指标值 | 24 / 30px | 等宽数字；附真实单位或统计口径 |

文案规则：

- 页面标题命名对象，例如“教学总览”“MCP Server”；禁止“把……收敛成……”等口号作工作页标题。
- 页头说明最多一句，只说明任务范围或必要条件；模块名、徽标、标题不连续重复同一事实。
- 按钮用动作加对象：“开始提问”“审核记录”“导出 SFT”；对象明确时可用“保存”“取消”。
- 空状态写现状与下一步；错误写影响与可执行动作。不把服务端配置指导交给学生。
- 教师端统一“备课问答”“回答审核”“待审核”“已审核”；学生端统一“学习提问”“挑战练习”“学习记录”。
- 管理员统一“模型供应商”“MCP Server”“Prompt 预设”“SFT / DPO 导出”“运行日志”；保留 API Key、Base URL、Model ID、Token、TTFT 等实际存在的技术概念，不添加无数据来源的指标。
- 技术约束只在相关字段或操作前出现一次。必填、数据用途、导出过滤、破坏性后果不能因精简被删。
- 禁止无证据的初始密码规则、效果承诺、实时状态、掌握程度和“就绪即健康”表述。

## 4. 布局与多端

沿用 4px 间距基准及 Tailwind 断点：640 / 768 / 1024 / 1280px。内容最大宽度 `max-w-7xl`，页面边距手机 16px、平板 24px、桌面 32px。工作页区块间距 24–32px。

- 登录：桌面品牌与表单双栏；手机为品牌简述、表单、使用说明。默认不抢输入焦点，避免手机自动弹出键盘。
- 仪表盘：紧凑标题、待办或异常、关键指标、工作列表。相同指标同屏只出现一次。没有数据时展示空状态，不填充演示数字。
- 学生：保留对话主路径，顶部持续展示提问、挑战、记录入口；个人菜单承载账号操作。核心学习路径不能只藏在头像里。
- 教师与管理端：桌面默认展开带文字导航；手机使用抽屉。导航、面包屑与页面标题使用一致词汇。
- 桌面侧栏在栏内顶部统一展开/收起，收起宽度 64px，导航点击区域 44px、图标 20px，居中排列并保留 tooltip；页头只在手机显示菜单入口，选中导航后关闭抽屉。
- 日志按功能、执行结果、时间和处理建议展示；开发定位信息按需展开并可复制，不在页面展示启动命令、文件路径或数据库读取策略。仅有采样日志时明确统计范围。
- 个人菜单提供账号设置，个人密码与头像由本人管理；账号角色、班级和停用状态继续由校方管理。
- 列表与配置页：短页头、操作、列表；长规则放在相关字段旁。表格在自身容器横向滚动，不撑宽整页。
- 对话区保留原滚动边界和输入区位置。长中文标题可换行；技术 ID 可以滚动或换行，不能挤出操作按钮。

## 5. 复用组件与状态

| 组件 | 结构 / 状态 | 约束 |
| --- | --- | --- |
| `WorkspaceHero` | H1、可选说明、主次操作、指标条、可选子内容 | 作为工作页头使用；无指标时不预留空栏；指标为静态信息，无悬停升起效果 |
| `SectionHeader` | H2、可选说明和操作 | 与页头拉开字号层级，手机操作换行 |
| `AppShell` / `AppSidebar` | 角色导航、当前位置、个人菜单、退出错误 | 链接标记 `aria-current`；跳过导航入口；图标不能替代可见标签 |
| `Button` / `Input` / `Label` | 默认、hover、focus、disabled、loading | 使用现有 Base UI 原语；保留键盘操作、明确 label 和忙碌状态 |
| `Card` / `Badge` | 对象分组、状态标签 | 仅对独立对象使用卡片；静态指标不伪装成链接 |
| `EmptyState` / `ErrorState` | 无记录 / 加载失败 | 区分零数据和加载失败；不输出虚构结论 |
| `RoleBadge` | 学生 / 教师 / 管理员 | 语义色及完整颜色变量；文字标识始终保留 |

## 6. 动效与交互

沿用现有 150–200ms 按钮与菜单反馈；不添加装饰动画。保持焦点可见、加载期间阻止重复提交、失败后允许重试。尊重 `prefers-reduced-motion`，不以动画表达唯一状态。

## 7. 层次与表面

沿用 8px 基础圆角、`shadow-soft` / `shadow-ink` 语义阴影。登录页允许品牌柔光；工作页头采用边界与留白，去除超大光斑、嵌套指标卡与宣传页式排版。学生阅读内容保留文化排版，管理员标题回归无衬线以便扫描配置。

## 8. 可访问性与验收限制

- 关键文字对比度目标 4.5:1，焦点与大字至少 3:1；辅助文字变深，禁止靠降低透明度削弱关键说明。
- 主触控目标至少 44px；表单有显式标签，错误使用 alert，选中状态同时有文字或结构标记。
- 验收视口：375、768、1280px；检查长标题、零数据、加载失败、菜单、Tab 焦点与表单错误。
- 用户已授权运行本地 Web；用实际页面验证导航、个人设置和日志交互，未覆盖的真实数据操作必须如实记录。
- 已有基础组件继续复用，不为这轮文案和层级优化安装额外开发工具或变更部署配置。
